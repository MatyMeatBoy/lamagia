{
  "name": "@prossh/match-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@prossh/rules": "*",
    "@fastify/cors": "^11.0.1",
    "fastify": "^5.2.1",
    "socket.io": "^4.8.1"
  },
  "devDependencies": { "@types/node": "^22.10.2", "tsx": "^4.19.2" }
}
