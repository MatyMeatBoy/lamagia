# Work claims

Client scope extension (2026-09-04): `client-mana-images` also owns creature-only P/T display in main.ts plus focused display helper/tests. Darwin owns authoritative current-creature projection; client honors that flag over printed types. main.ts retained until image/stats commit, then released for Undo UI.

CLIENT IMAGE CLAIM (2026-09-04): Astra owns `client-mana-images`: apps/client/src/main.ts existing image patch, focused image helper/tests, Vite types/config, and apps/client/src/assets/mana copies of referenced SVGs (public assets read-only, never staged). Base 470cad0cfc7e58c29d5c174de4354d1818ea4db6. Active; shared integration, no push per user.

This is the lightweight coordination ledger for parallel card and rules work.
The claim key is a reusable primitive or a disjoint card batch, never a vague
feature name.
| Cluster | Worker branch | Scope | Status |
| --- | --- | --- | --- |
| `c13-until-end-turn-creatures` | `codex/local-c13` | Sudden Spoiling: remove abilities and set target player's creatures to base 0/2 until end of turn | active |
| `c13-linked-exile-return` | `codex/c13-aura-granted-activation-7bd` | Fiend Hunter-style ETB exile of another nontoken creature with a linked leaves-the-battlefield return under its owner's control (CR 603.6c, 400.7) | active |
| `global-affinity-artifacts` | `feat/activated-abilities-and-triggers` | Reusable Affinity for artifacts cost reduction for artifact spells and affected cards (CR 702.41, 118.9) | active |
| `c13-aura-control-static` | `codex/c13-aura-granted-activation-7bd` | Reusable static Aura control primitive for `You control enchanted creature/land/permanent` (CR 303.4, 611.3, 613.7) | active |
| `c13-oracle-damage-activated-cost` | `feat/activated-abilities-and-triggers` | Reusable self-sacrifice activation that damages attacking creatures without flying; Leonin Bladetrap | merged (`5ceb67a`) |
| `c13-modern-self-reference-etb-sweep` | `feat/activated-abilities-and-triggers` | Normalize modern `this creature enters` Bane of Progress wording into the existing artifact/enchantment sweep-and-counters primitive | merged (`93ce7d4`) |
| `c13-life-gained-counter-scaling` | `feat/activated-abilities-and-triggers` | Reuse life-gained event amount for Cradle of Vitality's optional paid counters | merged (`93ce7d4`) |
| `c13-sacrifice-nontoken-artifact-token-life` | `feat/activated-abilities-and-triggers` | Reuse typed nontoken-artifact activation costs and compound token/life resolution for Thopter Foundry | merged (`a154e2f`) |
| `c13-whenever-deals-damage-opponent` | `codex/local-c13` | Reusable any-damage-to-opponent event for Charnelhoard Wurm's graveyard-return trigger | active |
| `c13-remove-counter-from-deals` | `codex/local-c13` | Reusable activated cost for removing a counter, then dealing parameterized damage to any legal target; Deathbringer Thoctar | active |
| `c13-oracle-gain-life-triggered-3` | `codex/c13-gladehart` | Reusable landfall trigger with optional life gain, including the modern “enters the battlefield under your control” wording (CR 603.1, 603.2, 118.3) | merged (`667ddb1`) |
| `c13-oracle-counter-static-spell` | `codex/c13-spellbreaker` | Spellbreaker Behemoth: creature spells you control with power 5 or greater can't be countered (CR 101.2, 601.2, 608.2b) | merged (`49fd8a7`) |
| `c13-oracle-exile-triggered-target` | `codex/c13-flickerwisp-final` | Flickerwisp: exile another target permanent and return it under its owner's control at the beginning of the next end step (CR 603.2, 603.3d, 603.7, 400.7) | merged (`8c34a4a`) |
| `rules-equipment` | `codex/c13-equipment-cluster` | Equip, attachment, Equipment static bonuses, and Sword of the Paruns untap abilities | Ready for integrator review |
| `rules-c13-reprint-equivalence` | `codex/c13-equipment-cluster` | Verify C13 reprints reuse existing oracle-driven rules for Command Tower and Decree of Pain; track Army of the Damned's Flashback gap | Ready for integrator review |
| `rules-flashback` | `codex/c13-equipment-cluster` | Flashback cost parsing, graveyard casting, and exile replacement for instant and sorcery cards | Ready for integrator review |
| `rules-c13-multi-basic-search` | `codex/c13-equipment-cluster` | Multi-card basic-land searches with ordered destinations for Cultivate and Armillary Sphere | Ready for integrator review |
| `rules-c13-life-gain-counter` | `codex/c13-equipment-cluster` | Reuse the life-gained trigger and +1/+1 counter primitive for Ajani's Pridemate | Ready for integrator review |
| `rules-c13-shuffle-source` | `codex/c13-equipment-cluster` | Shuffle-self replacement for Blue Sun's Zenith after its target draw resolves | Ready for integrator review |
| `rules-c13-scry` | `codex/c13-equipment-cluster` | Reusable Scry N primitive: private top-card projection, arbitrary top/bottom ordering, duplicate-name-safe ordinal choices, and C13 New Benalia Scry 1 | Ready for integrator review |
| `rules-c13-landfall-pump` | `codex/c13-equipment-cluster` | Reusable trigger-self P/T plus temporary keyword effect for Landfall cards such as Baloth Woodcrasher | Ready for integrator review |
| `rules-c13-basalt-untap` | `codex/c13-equipment-cluster` | Reusable source-untap activation and static “doesn't untap during your untap step” rule for Basalt Monolith | Ready for integrator review |
| `rules-c13-reuse-basic-effects` | `codex/c13-equipment-cluster` | Verify C13 cards reuse existing draw, damage, sacrifice-cost, and upkeep compound primitives: Borrowing 100,000 Arrows, Blood Rites, Carnage Altar, Baleful Force | Ready for integrator review |
| `rules-c13-tap-typed-cost` | `codex/c13-equipment-cluster` | Reusable activation cost for tapping an untapped creature or subtype you control, applied to Azami, Lady of Scrolls | Ready for integrator review |
| `rules-c13-draw-spell-reuse` | `codex/c13-equipment-cluster` | Verify C13 draw spells reuse the existing draw primitives: Brilliant Plan, Harmonize, Vision Skeins, and Deep Analysis with Flashback | Ready for integrator review |
| `rules-c13-baleful-strix-etb` | `codex/c13-equipment-cluster` | Apply the existing ETB draw and combat-keyword primitives to the C13 Baleful Strix printing | Ready for integrator review |
| `rules-c13-etb-draw-life` | `codex/c13-equipment-cluster` | Reuse the compound draw-and-life-loss effect for the C13 Phyrexian Gargantua ETB | Ready for integrator review |
| `rules-c13-annihilate-draw` | `codex/c13-equipment-cluster` | Verify Annihilate reuses typed nonblack-creature destruction plus the shared draw effect | Ready for integrator review |
| `rules-c13-etb-graveyard-exile` | `codex/c13-equipment-cluster` | Reuse target-player graveyard exile and existing ETB/land primitives for Angel of Finality and Bojuka Bog | Ready for integrator review |
| `rules-c13-arcane-denial-delay` | `codex/c13-equipment-cluster` | Reusable delayed-upkeep draw effects created by Arcane Denial after it counters a spell | Ready for integrator review |
| `rules-c13-bane-of-progress` | `codex/c13-equipment-cluster` | Reusable ETB sweep of artifacts/enchantments plus counters for permanents destroyed, applied to Bane of Progress | Ready for integrator review |
| `rules-c13-augur-top-selection` | `codex/c13-equipment-cluster` | Reusable private top-N selection: optionally take one matching card and bottom-order the rest, applied to Augur of Bolas | Ready for integrator review |
| `rules-c13-act-of-authority-control` | `codex/c13-equipment-cluster` | Reuse typed artifact/enchantment exile and transfer the enchantment to the exiled permanent's controller on the upkeep trigger | Ready for integrator review |
| `rules-level-up` | `codex/c13-equipment-cluster` | Level up activation, level counters, and level-band P/T/keyword characteristics | Ready for integrator review |
| `rules-tap-untap` | `codex/c13-equipment-cluster` | Targeted Tap target creature and Untap target permanent effects | Ready for integrator review |
| `rules-mill` | `codex/c13-equipment-cluster` | Target player mills a bounded number of cards into their graveyard | Ready for integrator review |
| `rules-counter-restrictions` | `codex/c13-equipment-cluster` | Creature-spell and noncreature-spell counter target families | Ready for integrator review |
| `rules-plus-counters` | `codex/c13-equipment-cluster` | Put +1/+1 or -1/-1 counters on a target creature | Ready for integrator review |
| `rules-target-discard` | `codex/c13-equipment-cluster` | Target-player discard with an explicit hand-card choice | Ready for integrator review |
| `rules-life-gained` | `codex/c13-equipment-cluster` | Life-gained trigger event and source +1/+1/-1/-1 counter effect | Ready for integrator review |
| `rules-target-life` | `codex/c13-equipment-cluster` | Target-player life gain with event propagation | Ready for integrator review |
| `rules-each-life` | `codex/c13-equipment-cluster` | Living-player life gain with one event per recipient | Ready for integrator review |
| `rules-target-life-loss` | `codex/c13-equipment-cluster` | Target-player life loss, distinct from damage | Ready for integrator review |
| `rules-each-life-loss` | `codex/c13-equipment-cluster` | Living-player global life loss, distinct from damage | Ready for integrator review |
| `rules-self-life-loss` | `codex/c13-equipment-cluster` | Controller life loss, distinct from damage | Ready for integrator review |
| `rules-life-lost-trigger` | `codex/c13-equipment-cluster` | Life-lost event bus for effects and damage | Ready for integrator review |
| `rules-token-creation` | `codex/c13-equipment-cluster` | Token creation preserves tapped state for C13 token family | Ready for integrator review |
| `rules-graveyard-return` | `codex/c13-equipment-cluster` | Targeted creature-card return from own graveyard to hand | Ready for integrator review |
| `rules-combat-damage-any-creature` | `codex/c13-equipment-cluster` | Combat-damage trigger for any creature source | Ready for integrator review |
| `rules-token-scaling` | `codex/c13-equipment-cluster` | Token creation scaled by lands controlled | Ready for integrator review |
| `rules-subtype-counters` | `codex/c13-equipment-cluster` | Counters on all controlled creatures of a subtype | Ready for integrator review |
| `rules-graveyard-exile` | `codex/c13-equipment-cluster` | Exile a targeted card from own graveyard | Ready for integrator review |
| `rules-x-opponent-loss` | `codex/c13-equipment-cluster` | X-scaled life loss for each opponent | Ready for integrator review |
| `rules-x-draw` | `codex/c13-equipment-cluster` | X-scaled self draw effect | Ready for integrator review |
| `rules-each-opponent-mill` | `codex/c13-equipment-cluster` | Global mill for each opponent | Ready for integrator review |
| `rules-each-opponent-draw` | `codex/c13-equipment-cluster` | Global draw for each opponent | Ready for integrator review |
| `rules-graveyard-library-top` | `codex/c13-equipment-cluster` | Return own graveyard card to library top | Ready for integrator review |
| `rules-all-creature-counters` | `codex/c13-equipment-cluster` | Counters on all creatures controlled by the caster | Ready for integrator review |
| `rules-creature-spell-trigger` | `codex/c13-equipment-cluster` | Creature-spell filter for cast triggers | Ready for integrator review |
| `rules-x-discard` | `codex/c13-equipment-cluster` | X-scaled private discard choices | Ready for integrator review |
| `rules-graveyard-battlefield` | `codex/c13-equipment-cluster` | Return a creature card from own graveyard to battlefield | Ready for integrator review |
| `rules-artifact-graveyard-target` | `codex/c13-equipment-cluster` | Restrict artifact-card recovery to artifact cards | Ready for integrator review |
| `rules-land-graveyard-battlefield` | `codex/c13-equipment-cluster` | Put a land from any graveyard onto battlefield under caster control | Ready for integrator review |
| `rules-artifact-graveyard-battlefield` | `codex/c13-equipment-cluster` | Return an artifact from own graveyard to battlefield | Ready for integrator review |
| `rules-enchantment-graveyard-target` | `codex/c13-equipment-cluster` | Restrict enchantment-card recovery to enchantment cards | Ready for integrator review |
| `rules-temporary-keyword` | `codex/c13-equipment-cluster` | Temporary keyword grants on targeted creatures | Ready for integrator review |
| `rules-temporary-pt-keyword` | `codex/c13-equipment-cluster` | Combined temporary P/T and keyword grant | Ready for integrator review |

Workers must claim a disjoint primitive or card batch before editing. The
integrator owns merge order and reruns coverage after each accepted commit.

## Before editing

1. Read this file, `AGENTS.md` and `docs/HANDOFF_TO_CLAUDE.md`.
2. Search open PRs and this table for the same claim key, card Scryfall IDs or
   engine files.
3. If free, add a row and publish that claim commit immediately.
4. Work only inside the declared scope. The first published claim wins; a
   later worker stops or chooses another batch.

## Active claims

Gameplay assignment: `gameplay-autopass-safe-undo` owns reusable counter-response relevance, safe mana undo rules/server endpoint and private projection, focused scenarios/docs. Base `470cad0cfc7e58c29d5c174de4354d1818ea4db6`; active 2026-09-04. Excludes client main.ts/styles. Explicit user assignment supersedes random card allocation; local commit only, no push.

| Claim key | Scope | Branch / PR | Status | Since (UTC) |
| --- | --- | --- | --- | --- |
| `global-exalted` | Exalted sole-attacker trigger and temporary pump (CR 702.83) | `feat/activated-abilities-and-triggers` | merged (`a9c3c45`) | 2026-09-04 |
| `global-shadow` | Shadow evasion and blocking symmetry (CR 702.28) | `feat/activated-abilities-and-triggers` | merged (`c5eeb6e`) | 2026-09-04 |
| `global-changeling` | Changeling keyword coverage using the existing all-creature-types model (CR 702.73) | `feat/activated-abilities-and-triggers` | merged (`8d2d186`) | 2026-09-04 |
| `global-prowess` | Prowess noncreature-spell trigger and temporary self pump (CR 702.108) | `feat/activated-abilities-and-triggers` | merged (`8d53f24`) | 2026-09-04 |
| `c13-horsemanship` | Global Horsemanship evasion keyword and Lu Xun C13 regression scenario (CR 702.31) | `feat/activated-abilities-and-triggers` | merged (`76b1d78`) | 2026-09-04 |
| `rules-land-search` | Landcycling variants and land-subtype search resolution | `feat/activated-abilities-and-triggers` | merged (`7c7f77c`) | 2026-09-03 |
| `rules-equipment` | Equip actions, attachment state, and Equipment static bonuses | `codex/c13-equipment-cluster` | merged (`f61a096`) | 2026-09-03 |
| `rules-c13-reprint-equivalence` | Verify C13 reprints reuse existing oracle-driven rules for Command Tower and Decree of Pain; track Army of the Damned's Flashback gap | `codex/c13-equipment-cluster` | merged (`44d0e78`) | 2026-09-04 |
| `rules-flashback` | Flashback alternative-cost casting, graveyard action visibility, and exile replacement | `codex/c13-equipment-cluster` | merged (`44d0e78`) | 2026-09-04 |
| `rules-flashback-life-cost` | Parse and pay life bundled into Flashback alternative costs, including C13 Deep Analysis | `feat/activated-abilities-and-triggers` | merged (`2b856c6`) | 2026-09-04 |
| `rules-c13-multi-basic-search` | Multi-card basic-land searches with ordered destinations for Cultivate, Armillary Sphere, and Burnished Hart | `codex/c13-equipment-cluster` | merged (`a4883df`) | 2026-09-04 |
| `rules-c13-self-shuffle` | Self-shuffling spell resolution and life-gain/+1/+1 trigger reuse, including Blue Sun's Zenith | `codex/c13-equipment-cluster` | merged (`3f6c2ec`) | 2026-09-04 |
| `c13-hua-tuo-graveyard-top` | Hua Tuo's tapped activation: put a target creature card from your graveyard on top of your library | `feat/activated-abilities-and-triggers` | merged (`f65d252`) | 2026-09-04 |
| `c13-global-spell-cost-reduction` | Global instant/sorcery spell cost reductions, including Arcane Melee; reusable for equivalent Oracle text | `feat/activated-abilities-and-triggers` | merged (`01a1a07`) | 2026-09-04 |
| `rules-c13-scry` | Scry N private ordered choices, including chained draw and ETB resolution | `codex/c13-equipment-cluster` | merged (`94eb93d`) | 2026-09-04 |
| `rules-c13-landfall-pump` | Triggered source self-pump (`~ gets +N/+N`, optional keyword) with reusable source identity | `codex/c13-equipment-cluster` | merged (`285a34c`) | 2026-09-04 |
| `c13-attack-limit` | Static defender-side limit on the number of creatures that can attack a player each combat; Crawlspace | `feat/activated-abilities-and-triggers` | merged (`9ee30ae`) | 2026-09-04 |
| `c13-basalt-untap` | Activated self-untap plus static no-untap-during-untap rule, including Basalt Monolith | `codex/c13-equipment-cluster` | merged (`e587d1d`) | 2026-09-04 |
| `c13-reuse-basic-effects` | Reuse draw-per-tapped-creature, typed sacrifice damage/draw, and upkeep compound triggers for four C13 cards | `codex/c13-equipment-cluster` | merged (`8474a06`) | 2026-09-04 |
| `c13-typed-tap-cost` | Reusable `Tap an untapped [subtype] you control` activation costs with `any`/`another` selection and server validation; Azami | `codex/c13-equipment-cluster` | merged (`c50a721`) | 2026-09-04 |
| `c13-druidic-satchel-top-reveal` | Reveal-top-card conditional: create a Saproling for creatures, put lands onto the battlefield, otherwise gain life | `feat/activated-abilities-and-triggers` | merged (`abd93f1`) | 2026-09-04 |
| `c13-etb-sacrifice-unless-pay` | ETB trigger that sacrifices its source unless its controller pays a fixed mana cost | `feat/activated-abilities-and-triggers` | merged (`51435d3`) | 2026-09-04 |
| `c13-draw-and-draw-life-reuse` | Reuse draw-only and draw/life compound primitives for Brilliant Plan, Harmonize, Vision Skeins, Deep Analysis and Phyrexian Gargantua | `feat/activated-abilities-and-triggers` | merged (`761c675`) | 2026-09-04 |
| `c13-baleful-strix-etb` | Baleful Strix ETB draw plus flying/deathtouch through existing primitives | `feat/activated-abilities-and-triggers` | merged (`5b397e8`) | 2026-09-04 |
| `c13-annihilate-destroy-draw` | Annihilate typed nonblack-creature removal followed by draw, with target legality scenarios | `feat/activated-abilities-and-triggers` | merged (`8d6c17b`) | 2026-09-04 |
| `c13-storage-mana` | Molten Slagheap and Saltcrusted Steppe storage counters: variable B/R or G/W output after paying {1} | `feat/activated-abilities-and-triggers` | active | 2026-09-04 |
| `c13-additional-life-cost` | Parse and pay spell additional life costs, starting with Toxic Deluge; reusable independently of the spell effect | `feat/activated-abilities-and-triggers` | merged (`704f234`) | 2026-09-04 |
| `c13-delayed-draw-choice` | Arcane Denial's delayed upkeep draws with private 0..N choice, deterministic bot selection and library clamping | `feat/activated-abilities-and-triggers` | merged (`a439b56`) | 2026-09-04 |
| `c13-graveyard-exile-etb` | ETB exile all graveyards for Angel of Finality and Bojuka Bog, reusing zone-exile primitives | `feat/activated-abilities-and-triggers` | merged (`054e82a`) | 2026-09-04 |
| `c13-split-second` | Enforce Split second's prohibition on non-mana spells and activated abilities while the spell is on the stack | `feat/activated-abilities-and-triggers` | merged (pending commit) | 2026-09-04 |
| `c13-protection` | Parse color protection and enforce its targeting, blocking, and combat-damage prevention rules; Sphinx of the Steel Wind | `feat/activated-abilities-and-triggers` | merged (`2c228ee`) | 2026-09-04 |
| `c13-destroy-target-nonblack-creatures` | Reusable multiple-target nonblack creature destruction with a controller life-loss follow-up; Reckless Spite | `feat/activated-abilities-and-triggers` | merged (`pending`) | 2026-09-04 |
| `c13-prevent-all-combat-damage` | Reuse combat-damage prevention for damage dealt to the source creature only; Guard Gomazoa | `feat/activated-abilities-and-triggers` | merged (`pending`) | 2026-09-04 |
| `c13-put-target-nonland-permanent` | Put a targeted nonland permanent beneath X cards of its owner's library; Unexpectedly Absent | `feat/activated-abilities-and-triggers` | active | 2026-09-04 |
| `rules-level-up` | Level up costs, level counters, and the three C13 cards: Echo Mage, Hada Spy Patrol, Kazandu Tuskcaller | `codex/c13-equipment-cluster` | merged (`f325052`) | 2026-09-03 |
| `rules-tap-untap` | Targeted Tap target creature and Untap target permanent effects | `codex/c13-equipment-cluster` | merged (`4fa0290`) | 2026-09-03 |
| `rules-mill` | Target player mills a bounded number of cards into their graveyard | `codex/c13-equipment-cluster` | merged (`2842700`) | 2026-09-03 |
| `rules-counter-restrictions` | Creature-spell and noncreature-spell counter target families | `codex/c13-equipment-cluster` | merged (`dcc9ada`) | 2026-09-03 |
| `rules-plus-counters` | Put +1/+1 or -1/-1 counters on a target creature | `codex/c13-equipment-cluster` | merged (`f026bdd`) | 2026-09-03 |
| `rules-target-discard` | Target player chooses bounded cards from their hand to discard | `codex/c13-equipment-cluster` | merged (`90e0098`) | 2026-09-03 |
| `rules-life-gained` | Life-gained trigger event and source +1/+1/-1/-1 counter effect | `codex/c13-equipment-cluster` | merged (`b1bf642`) | 2026-09-03 |
| `rules-target-life` | Target-player life gain with event propagation | `codex/c13-equipment-cluster` | merged (`41bfb46`) | 2026-09-03 |
| `rules-draws-card-trigger` | `draws-card` GameEvent/TriggerEvent family (Sheoldred/Nekusar/Underworld Dreams/Psychosis Crawler style "whenever you/an opponent draws a card" payoffs), `cantBeCountered`, instant-or-sorcery `spell-cast` trigger subtype, `your-hand-size` P/T CDA, and an `assertInvariants` fix in `tools/simulator/run_engine_matrix.ts` (a stack object backed by a triggered/activated ability reuses its source permanent's still-on-battlefield card and was being double-counted as an in-transit spell) | `c14-batch2-clean` | pending | 2026-09-04 |
| `rules-each-life` | Living-player life gain with one event per recipient | `codex/c13-equipment-cluster` | merged (`283a02f`) | 2026-09-03 |
| `rules-two-sentence-enters-tapped` | Consume the two-sentence "As ~ enters, you may pay/reveal ... . If you don't, it enters tapped." shock-land/reveal-land wording once `entersTapped` already captures the same replacement (Blood Crypt, Steam Vents, Watery Grave, Choked Estuary, and the wider shock/reveal land cycles) | `c14-batch2-clean` | pending | 2026-09-04 |
| `rules-target-life-loss` | Target-player life loss, distinct from damage | `codex/c13-equipment-cluster` | merged (`93318e6`) | 2026-09-03 |
| `rules-each-life-loss` | Living-player global life loss, distinct from damage | `codex/c13-equipment-cluster` | merged (`6d128b6`) | 2026-09-03 |
| `rules-self-life-loss` | Controller life loss, distinct from damage | `codex/c13-equipment-cluster` | merged (`67f92db`) | 2026-09-03 |
| `rules-life-lost-trigger` | Life-loss event bus for `Whenever you lose life` | `codex/c13-equipment-cluster` | merged (`3d4ab69`) | 2026-09-03 |
| `rules-token-creation` | Preserve tapped state on created tokens | `codex/c13-equipment-cluster` | merged (`19ae957`) | 2026-09-03 |
| `rules-graveyard-return` | Target card return from own graveyard to hand | `codex/c13-equipment-cluster` | merged (`ff80bd1`) | 2026-09-03 |
| `rules-graveyard-target-ui` | Visible player selector for legal graveyard card targets | `codex/c13-equipment-cluster` | merged (`c518638`) | 2026-09-03 |
| `rules-any-creature-combat` | Combat-damage triggers from any creature | `codex/c13-equipment-cluster` | merged (`d554c36`) | 2026-09-03 |
| `rules-landscaled-tokens` | Token amounts scaled by lands controlled | `codex/c13-equipment-cluster` | merged (`00be19b`) | 2026-09-03 |
| `rules-subtype-counters` | Counter effects across a creature subtype | `codex/c13-equipment-cluster` | merged (`015e495`) | 2026-09-03 |
| `rules-exile-graveyard` | Exile a target card from its controller's graveyard | `codex/c13-equipment-cluster` | merged (`85f46f8`) | 2026-09-03 |
| `rules-x-life-loss` | X-scaled opponent life loss | `codex/c13-equipment-cluster` | merged (`1d6be6d`) | 2026-09-03 |
| `rules-x-draw` | X-scaled card draw | `codex/c13-equipment-cluster` | merged (`47e23f6`) | 2026-09-03 |
| `rules-opponent-mill` | Each opponent mills a bounded amount | `codex/c13-equipment-cluster` | merged (`5311b7f`) | 2026-09-03 |
| `rules-opponent-draw` | Each opponent draws a bounded amount | `codex/c13-equipment-cluster` | merged (`4b0fa92`) | 2026-09-03 |
| `rules-graveyard-top` | Return a target graveyard card to library top | `codex/c13-equipment-cluster` | merged (`fa5b4bd`) | 2026-09-03 |
| `rules-all-creature-counters` | Put counters on all controlled creatures | `codex/c13-equipment-cluster` | merged (`160e3b4`) | 2026-09-03 |
| `rules-python-ir` | Reusable raw-text operands: actions, zones, card types, and subtypes for the Oracle compiler | `feat/activated-abilities-and-triggers` | merged (`7326cbe`) | 2026-09-03 |
| `tools-primitive-roadmap` | `tools/rules/plan_primitive_roadmap.py` and its unit tests: rank Oracle primitives by cards actually finished | `codex-ready/c14-combat-restrictions` | merged (`3677066`) | 2026-09-03 |
| `c14-combat-restrictions` | Printed can't attack / can't block / attacks each combat if able / can block only creatures with X | `codex-ready/c14-combat-restrictions` | merged (`3677066`) | 2026-09-03 |
| `c14-landwalk` | Landwalk evasion for every basic land type plus legendary landwalk | `codex-ready/c14-combat-restrictions` | merged (`3677066`) | 2026-09-03 |
| `c14-self-pump` | Firebreathing-style `{cost}: ~ gets +N/±N until end of turn` self activated pumps (`modify-source-creature` effect) | `c14-self-pump` | merged (`8d88bbb`) | 2026-09-03 |
| `c14-scry` | `Scry N` / `Scry N, then draw M` as a spell effect and ETB trigger; sequential keep/bottom pending choice | `c14-self-pump` | merged (`ead5da1`) | 2026-09-03 |
| `c14-combat-damage-target` | `~ deals N damage to target attacking or blocking creature` (`attacking-or-blocking-creature` target kind) | `c14-self-pump` | merged (`ead5da1`) | 2026-09-03 |
| `c14-it-deals-trigger` | Normalise leading "it deals/gets/gains/fights" in a trigger clause to the source (`~`) | `c14-self-pump` | merged (`cbdceac`) | 2026-09-03 |
| `c14-damage-sweep-filter` | `~ deals N damage to each nonartifact creature` / `each creature without flying` | `c14-self-pump` | merged (`cbdceac`) | 2026-09-03 |
| `c14-destroy-permanent-and-self-bounce` | plain `Destroy target permanent`; `Return a creature you control to its owner's hand` | `c14-batch2-clean` | ready (`d3dae63`) | 2026-09-04 |
| `c14-board-cost-reduction` | `~ costs {N} less to cast for each creature on the battlefield` (negative additionalGeneric) | `c14-batch2-clean` | ready (`8def468`) | 2026-09-04 |
| `c14-medallion-cost-reduction` | `<color/type> spells you cast cost {N} less to cast` static grant | `c14-batch2-clean` | ready (`5a6b506`) | 2026-09-04 |
| `c14-graveyard-self-return` | `When ~ is put into a graveyard from the battlefield, return it to its owner's hand`; `You draw N cards and you lose N life` | `c14-batch2-clean` | ready (`ccc5268`) | 2026-09-04 |
| `c14-tapped-wipe-and-commander-mana` | `Destroy all tapped creatures`; `Add N mana of any color in your commander's color identity` | `c14-batch2-clean` | ready (`727fe47`) | 2026-09-04 |
| `c14-evoke` | Evoke alternative cost + kicked-style sacrifice trigger (CR 702.34) | `c14-batch2-clean` | ready (`95c912c`) | 2026-09-04 |
| `c14-extort` | Extort keyword synthesised as a spell-cast optional-pay drain trigger (CR 702.39) | `c14-batch2-clean` | ready (`4992925`) | 2026-09-04 |
| `c14-static-land-mana-bonus` | `<Basic type>s you control produce an additional {C}` / tap-for-mana wording | `c14-batch2-clean` | ready (`fa44b87`) | 2026-09-04 |
| `c14-planeswalker-loyalty` | Planeswalker loyalty counters, sorcery-speed loyalty abilities, 0-loyalty SBA; `draw a card for each <color> creature` | `c14-batch2-clean` | ready (`d98a87f`) | 2026-09-04 |
| `c14-enters-or-dies-and-leaves` | `When ~ enters or is put into a graveyard from the battlefield, X` (two triggers); `leaves the battlefield` aliased to the dies event | `c14-batch2-clean` | active | 2026-09-04 |
| `c14-target-cant-block` | `Target creature can't block this turn` (per-permanent flag cleared in cleanup) | `c14-batch2-clean` | active | 2026-09-04 |
| `c14-storm-attacker-sacrifice` | Consume Storm keyword text and resolve `Target player sacrifices an attacking creature of their choice` | `c14-batch2-clean` | merged (`893730c`) | 2026-09-04 |
| `c13-unblockable` | Printed `~ can't be blocked` combat restriction | `codex/c13-equipment-cluster` | merged (`4a883e1`) | 2026-09-03 |
| `c13-activated-sacrifice-creature` | Activated costs that sacrifice a creature or another creature | `codex/c13-equipment-cluster` | merged (`7c54447`) | 2026-09-03 |
| `c13-activated-remove-counters` | Activated costs that remove counters from the source permanent | `codex/c13-equipment-cluster` | merged (`96142f7`) | 2026-09-03 |
| `c13-global-temporary-keyword` | Static `Creatures you control have <keyword>` grants, including summoning-sickness checks and projection | `codex/c13-equipment-cluster` | merged (`a1b42d9`) | 2026-09-03 |
| `c13-life-equals-power` | Gain life equal to the current power of a target creature you control | `codex/c13-equipment-cluster` | merged (`8c76cc9`) | 2026-09-03 |
| `c13-compound-draw-life-loss` | Resolve a compound draw plus life-loss instruction as one effect | `codex/c13-equipment-cluster` | merged (`80c6d53`) | 2026-09-03 |
| `c13-hand-count-damage` | Resolve damage equal to the controller's hand size | `codex/c13-equipment-cluster` | merged (`969c85d`) | 2026-09-03 |
| `tools-parallel-workers` | Assign disjoint primitive clusters to bounded workers under the shared memory budget | `codex/c13-equipment-cluster` | merged (`96be7d7`) | 2026-09-03 |
| `rules-regeneration` | Regeneration shields, destruction replacement, combat removal, and reusable `{cost}: Regenerate ~` parsing | `feat/activated-abilities-and-triggers` | merged (`f30492d`) | 2026-09-03 |
| `c13-draw-step-additional-card` | Draw-step triggers that draw an additional card for the active player | `codex/c13-equipment-cluster` | merged (`e002034`) | 2026-09-03 |
| `c13-opponent-hand-minus-damage` | Opponent-upkeep damage equal to active player's hand count minus an offset | `codex/c13-equipment-cluster` | merged (`93bd5a2`) | 2026-09-03 |
| `c13-conditional-subtype-trigger` | Conditional triggers gated by controlling no permanent of a subtype | `codex/c13-equipment-cluster` | merged (`e352f7b`) | 2026-09-03 |
| `c13-draw-tapped-creatures` | Draw a card for each tapped creature controlled by a targeted opponent | `codex/c13-equipment-cluster` | merged (`493f587`) | 2026-09-03 |
| `c13-global-creature-keyword` | Temporary keyword grants to all creatures on the battlefield | `codex/c13-equipment-cluster` | merged (`7426cf7`) | 2026-09-03 |
| `c13-life-gain-prevention` | Static prevention of all player life gain | `codex/c13-equipment-cluster` | merged (`4fc6670`) | 2026-09-03 |
| `c13-no-maximum-hand-size` | Static removal of the cleanup maximum-hand-size discard | `codex/c13-equipment-cluster` | merged (`64dd7a3`) | 2026-09-03 |
| `c13-static-creature-pt-grant` | Static +P/+T bonuses for other creatures you control | `codex/c13-equipment-cluster` | merged (`5f1b014`) | 2026-09-03 |
| `c13-destroy-power-toughness-loss` | Destroy a creature then make its controller lose its power plus toughness | `codex/c13-equipment-cluster` | merged (`9ea3d05`) | 2026-09-03 |
| `c13-variable-global-debuff` | X-scaled -X/-X effects affecting every creature | `codex/c13-equipment-cluster` | merged (`cb9abba`) | 2026-09-03 |
| `c13-conditional-power-trigger` | Optional triggers gated by a controlled creature's power threshold | `codex/c13-equipment-cluster` | merged (`a5fb83a`) | 2026-09-03 |
| `c13-nonflying-global-damage` | X-scaled damage to nonflying creatures and each player | `codex/c13-equipment-cluster` | merged (`7216afe`) | 2026-09-03 |
| `c13-upkeep-draw-life-loss` | Upkeep draw-and-life-loss compound trigger wording | `codex/c13-equipment-cluster` | merged (`2efb489`) | 2026-09-03 |
| `c13-counted-effects-batch` | Reusable draw/life/damage/mill/discard/token primitives scaled by hand size, permanent type, creature count, lands, and power thresholds | `codex/c13-equipment-cluster` | merged (`77b235a`) | 2026-09-04 |
| `c13-keyword-target-filters` | Target filters for fear, defender, deathtouch, lifelink, menace, haste, first/double strike, trample, vigilance, indestructible, hexproof, and shroud; continuous grants are respected | `codex/c13-equipment-cluster` | merged (`3b2f825`) | 2026-09-04 |
| `rules-compound-sentence-order` | Parse multi-sentence destruction plus power/toughness life-loss effects before the generic destruction primitive | `feat/activated-abilities-and-triggers` | merged (`5e9f3cf`) | 2026-09-04 |
| `c13-fork-followup-20260904` | Reach and flying-only filters; global keyword/P+T layers; counted planeswalkers and battles; optional-condition narrowing; GameCard test fixtures | `codex/c13-equipment-cluster` | merged (local `b3372a2`; source `3cd6156`) | 2026-09-04 |
| `c13-cross-graveyard-targets` | Exile or return cards from any graveyard with reusable creature, artifact, enchantment, and land filters | `codex/c13-equipment-cluster` | merged (local `5bd0bff`) | 2026-09-04 |
| `c13-artifact-etb-trigger-subject` | Trigger subjects for artifacts entering under your control | `codex/c13-equipment-cluster` | merged (source `295e798`) | 2026-09-04 |
| `c13-enchantment-etb-trigger-subject` | Trigger subjects for enchantments entering under your control | `codex/c13-equipment-cluster` | merged (local `25a5bbf`) | 2026-09-04 |
| `rules-python-sacrifice-operands` | Preserve typed sacrifice operands and reusable primitive clusters in the Oracle compiler | `codex/c13-equipment-cluster` | merged (local `89e25d2`) | 2026-09-04 |
| `c13-activated-sacrifice-artifact` | Activated costs that sacrifice an artifact, including “another” | `codex/c13-equipment-cluster` | merged (local `e1a0518`) | 2026-09-04 |
| `c13-activated-sacrifice-enchantment` | Activated costs that sacrifice an enchantment, including “another” | `codex/c13-equipment-cluster` | merged (local `e1a0518`) | 2026-09-04 |
| `c13-activated-sacrifice-land` | Activated costs that sacrifice a land, including “another” | `codex/c13-equipment-cluster` | merged (local `e1a0518`) | 2026-09-04 |
| `c13-activated-sacrifice-noncreature` | Activated costs that sacrifice any noncreature permanent | `codex/c13-equipment-cluster` | merged (local `98df4aa`) | 2026-09-04 |
| `c13-activated-discard-cost` | Activated costs that discard one card from the controller’s hand | `codex/c13-equipment-cluster` | merged (local `bea78a5`) | 2026-09-04 |
| `rules-python-cost-actions` | Preserve reusable activated-cost actions (`discard`, `exile`, `sacrifice`) in Oracle IR clusters | `codex/c13-equipment-cluster` | merged (local `2ccfbed`) | 2026-09-04 |
| `c13-each-player-spell-trigger` | Triggered abilities that watch any player cast a spell | `codex/c13-equipment-cluster` | merged (local `a40b295`) | 2026-09-04 |
| `c13-trigger-subject-clusters` | Reuse generic permanent ETB subjects instead of rediscovering them per card | `feat/activated-abilities-and-triggers` | merged (local `df3db32`) | 2026-09-04 |
| `c13-generic-permanent-sacrifice-cost` | Activated costs that sacrifice any permanent, with server-side legal choice validation | `codex/c13-equipment-cluster` | merged (local `f821ede`) | 2026-09-04 |
| `c13-graveyard-permanent-return` | Return a permanent card from your or any graveyard to the battlefield | `codex/c13-equipment-cluster` | merged (local `eaa7159`) | 2026-09-04 |
| `c13-graveyard-permanent-exile` | Exile a permanent card from any graveyard with privacy-safe target filtering | `codex/c13-equipment-cluster` | merged (local `e38277a`) | 2026-09-04 |
| `c14-kicker` | Kicker/Multikicker cost (CR 702.33): kicked cast action, kicked-only effects and enters triggers | `c14-batch2-clean` | merged (`02b40ed`) | 2026-09-04 |
| `c14-optional-pay-trigger` | `you may pay {cost}. If you do, X` optional-cost trigger effects | `c14-batch2-clean` | merged (`02b40ed`) | 2026-09-04 |
| `c14-draw-then-discard` | `Draw N, then discard M` effect (spell + activated) + bot discard-cards policy | `c14-batch2-clean` | merged (`02b40ed`) | 2026-09-04 |
| `c14-self-zone` | `Exile ~` / `Shuffle ~ into its owner's library` spell self-destination; graveyard `another target` + nonland-permanent bounce | `c14-batch2-clean` | merged (`02b40ed`) | 2026-09-04 |
| `bot-fear-block` | Bot chooseBlockers respects fear (CR 702.36b) | `c14-batch2-clean` | merged (`8f095af`) | 2026-09-04 |
| `c14-any-creature-enters-trigger` | `Whenever another creature enters, X` — errata-modern trigger wording without "under your control" (watches every controller, still excludes the source itself); unlocks Essence Warden (C14) plus every Soul Warden/Wretched Anurid printing sharing the primitive | `worker-05` | pushed (`acda42e`) | 2026-09-04 |
| `rules-target-player-draw-and-lose-life` | `Target player draws N cards and loses N life` (Sign in Blood pattern) as a compound of the existing single-player draw and life-loss effects sharing one target; also resolves through the trigger path (Bloodgift Demon's upkeep clause) | `worker-05` | pushed (`bba88a4`) | 2026-09-04 |
| `rules-blood-artist-drain` | `Whenever ~ or another creature dies, target player loses N life and you gain N life` (Blood Artist / Falkenrath Noble): new `any-creature` dies-trigger wording that restores the source to its own death watch, plus a compound of the existing `lose-life-target-player` and `gain-life` effects | `worker-05` | pushed (`bd9d8a2`) | 2026-09-04 |
| `rules-partner-keyword` | `Partner` (CR 702.123, deck-construction only, no per-card state) and `Partner with <name>` (CR 702.124f: exact-name library search, decided by the *target* player, not the controller — new `choiceBy: "target"`); found in Battlebond, Commander 2016/2020, Commander Legends, Doctor Who, Tales of Middle-earth Commander (the LOTR hobbit pairs) and others, not Commander 2017 itself | `worker-05` | pushed (`bac94ad`) | 2026-09-04 |
| `rules-exile-and-bottom-library-lifegain` | Swords to Plowshares (`Exile target creature. Its controller gains life equal to its power.`) and Condemn (`Put target attacking creature on the bottom of its owner's library. Its controller gains life equal to its toughness.`): both capture power/toughness as last known information (CR 613.7a) before the creature leaves; new `attacking-creature` target kind and a generalized `movePermanentToZone` `"library-bottom"` zone | `worker-05` | pushed (`b220554`) | 2026-09-04 |
| `rules-event-player-drain` | `worker-05` | `Whenever an opponent draws a card, ~ deals N damage to that player/them` and `...that player loses N life`: "that player" is the card-drawn event's own player, not a chosen target, resolved from `object.trigger?.eventController`; extended `eventController` capture to "seat"-keyed events (card-drawn, life-gained/lost) alongside "controller"-keyed ones. Fully implements Nekusar the Mindrazer, Underworld Dreams, Scrawling Crawler, Spellshock, Iron Maiden, Calculating Lich, Fate Unraveler, Incite Rebellion, Viseling, Aether Sting; partially resolves Razorkin Needlehead | Ready for integrator review |
| `rules-shock-land-credit` | `worker-05` | Shock lands ("As ~ enters, you may pay 2 life. If you don't, it enters tapped.") were already fully enforced via the existing `entersTapped` `unless-pay-life` rule, but the printed line was never marked covered by the per-line parser, so every shock land stayed `fullyImplemented: false`. Added the missing consumption check (no engine change — the behavior already executed) plus its first scenario test. Fully implements Blood Crypt, Watery Grave, Steam Vents, Breeding Pool, Hallowed Fountain, Temple Garden, Overgrown Tomb, Sacred Foundry, Godless Shrine, Stomping Ground | Ready for integrator review |
| `rules-pain-mana-lifecost` | `worker-05` | "{T}: Add X or Y. ~ deals 1 damage to you." (pain lands, painful talismans) was a real bug, not just an uncredited line: the colored-mana ability parsed and produced mana, but silently dropped the damage clause, giving `lifeCost: 0` and free mana. `parseManaInstruction` now strips a trailing `~ deals N damage to you` the same way it already strips `You gain N life`, folding it into the ability's existing `lifeCost` field (already fully wired into activation legality and payment). Fully implements 30 cards: the full pain-land cycle (Adarkar Wastes, Battlefield Forge, Brushland, Caves of Koilos, Karplusan Forest, Llanowar Wastes, Shivan Reef, Sulfurous Springs, Underground River, Yavimaya Coast) and painful-talisman cycle (all 10), plus Ancient Tomb, Grand Coliseum, Tarnished Citadel, Fogwell's Gym, Elves of Deep Shadow, Scabland, Salt Flats, Pine Barrens, Skyshroud Forest, Caldera Lake | Ready for integrator review |
| `rules-damage-amplify` | `worker-05` | New static "damage amplifier" primitive (CR 614.1c): "If a [color] source you control would deal damage to [an opponent or a permanent an opponent controls / a permanent or player], it deals that much damage plus N instead." (Torbran, Thane of Red Fell and the modern "damage doubler" design space). New `CardProfile.damageAmplify` (`colorFilter?`, `excludesSelf` for "another", `scope: "opponent"\|"any"`, `amount`) plus `damageAmplifyBonus` summed across every amplifier the source's controller has in play, wired into both `dealDamageToPlayer`/`dealDamageFromObject` (spells/abilities — keyed off the object's own controller, not a permanent lookup, since a spell dealing its own damage has no matching permanent) and `dealDamageToPermanent` (8 call sites) plus combat damage to players and commander-damage tracking. Fully implements Torbran, Thane of Red Fell; Jaya, Venerated Firemage; Embermaw Hellion; Thor, Asgard's Avenger. LIMIT: lifelink on amplified combat damage still gains only the pre-amplification amount (`computeCombatDamage` computes lifelink before `applyCombatDamage` applies the bonus) — not fixed in this batch, no pending card in this run has both lifelink and the "another"/amplified interaction | Ready for integrator review |
| `rules-ritual-add-mana` | `worker-05` | The `"add-mana"` `SpellEffect` kind and its executor already existed fully wired in `engine.ts` (adds a fixed pool to the caster's mana pool) but had zero `recognizeSentence` wiring in `characteristics.ts`, so every ritual spell stayed unparsed. Added a pattern for the literal `Add {mana symbols}` sentence, reusing the existing `parseAddClause` helper and converting its result (either a single repeated color or a `fixedProduces` mix of distinct colors) into the effect's `pool` map. Deliberately unmatched: any ritual phrased as a player choice or "any color", since no printed ritual asks the caster to pick and that shape isn't modeled. Fully implements Dark Ritual, Pyretic Ritual, Seething Song, Channel the Suns, First Stage of Magic Design, Rapturous Moment, Liturgy of Blood, Seismic Spike, Deconstruct, Turn to Dust | Ready for integrator review |
| `rules-discard-event-trigger` | `worker-05` | New reusable event: "Whenever a player/an opponent discards a card, ~". No such event existed — discards were inline `hand`/`graveyard` mutations at 7 separate call sites (pending-choice discards, hand-size cleanup, activated-ability discard costs, `Windfall`-style discard-your-hand effects) with nothing raising a watchable event. Added `GameEvent`/`TriggerEvent` kind `card-discarded` plus a `discardCard`/`discardCards` helper (mirrors `loseLife`/`drawCards`) that performs the existing mutation and then `raiseEvent`s it; replaced all 7 inline mutation sites with calls to the helper, changing no discard selection/amount logic, only adding the missing event. `eventController` capture already generalized to any `seat`-keyed event, so `"that player loses/that player takes"` effect language resolves for free via the existing `lose-life-event-player`/`damage-event-player` effects. Fully implements Liliana's Caress, Megrim, Spirit Cairn, Geth's Grimoire, Abyssal Nocturnus | Ready for integrator review |
| `rules-you-draw-discard-trigger` | `worker-05` | Extended the `card-drawn`/`card-discarded` trigger-matching to the `"you"` subject (previously only `"each-player"`/`"opponent"` were wired, even though `"you"` already existed as a `TriggerSubject` for life-gain/loss triggers) — completes cards phrased "Whenever you draw/discard a card, you ...". No new effect executor needed: `"You gain N life"` already resolves off the ability's own controller, which for a `"you"`-subject trigger is exactly the player who drew/discarded. Also generalized the existing `"That player loses N life"` effect pattern to accept the equally common `"They lose N life"` phrasing (same `lose-life-event-player` effect, just a wording variant). Fully implements Sheoldred, the Apocalypse; Niv-Mizzet, the Firemind; Hobgoblin, Mantled Marauder; Chasm Skulker; The Value Knight; Spirit; Psychic Corrosion; Mystic Redaction; Clinquant Skymage; Oneirophage; Ravenhill Flock; Lyla, Holographic Assistant; Lorescale Coatl; Burlfist Oak; Horizon Chimera | Ready for integrator review |
| `rules-opponent-lands-any-color-mana` | `worker-05` | New reusable mana primitive: "{T}: Add one mana of any color that a land you/an opponent control(s) could produce" (Fellwar Stone, Harvester Druid). This clause was explicitly refused by `parseAddClause` (by design — a comment there warns against reading it as five free colors) and fell through to the `produced_mana` Scryfall fallback, which DID wrongly hand it a fixed 5-color list; a pre-existing test asserted that bogus behavior stayed `fullyImplemented: false`. Added `ManaAbility.anyColorFromLandsControlledBy: "opponent" \| "you"` (produces computed at activation time, not stored), a `colorsFromLandsControlledBy` helper, and threaded `state` through `manaOptionsFor`'s 3 call sites so it can inspect the right seats' battlefield lands. Found and fixed a real latent bug this surfaced: `applyChooseTrigger`'s optional-cost payment (engine.ts, the "may pay {N}" branch) called `planManaPayment` without `{ state }`, unlike its sibling branches a few lines above — harmless while no mana ability needed board state, but with this primitive live it let `legalActions` offer a payment `legalActions` itself would compute as affordable while the actual application recomputed a poorer (state-blind) source list and threw. Fixed by passing `{ state: next }` there too. Fully implements Fellwar Stone, Exotic Orchard, Sylvok Explorer, Quirion Explorer, Harvester Druid | Ready for integrator review |
| `rules-check-land-credit` | `worker-05` | Check lands ("As ~ enters, you may reveal a(n) <type(s)> card from your hand. If you don't, ~ enters tapped.") were already fully enforced via the pre-existing `entersTapped` `unless-reveal-card` rule and already had full scenario coverage upstream (Frostboil Snarl reveal/decline tests) — but, exactly like the shock-land fix, the printed line was never marked covered by the per-line parser, so the whole cycle stayed `fullyImplemented: false`. Added the missing consumption check (no engine change) plus a profile-level test asserting the credit. Fully implements Frostboil Snarl, Vineglimmer Snarl, Shineshadow Snarl, Furycalm Snarl, Necroblossom Snarl, Port Town, Foreboding Ruins, Game Trail, Fortified Village, Choked Estuary, Ancient Amphitheater, Gilt-Leaf Palace, Secluded Glen, Wanderwine Hub, Auntie's Hovel, Murmuring Bosk, Flamekin Village | Ready for integrator review |
| `rules-mana-entered-this-turn-restriction` | `worker-05` | New reusable mana-ability activation restriction: "Activate only if ~ entered the battlefield this turn (or if you control a basic land)" (Hidden Lair and its color-cycle siblings). Required a new tracked permanent field, `Permanent.enteredThisTurn` (mirrors `summoningSick`: stamped `true` in `putOntoBattlefield`, cleared to `false` for the active player's battlefield each untap step) since the engine had no per-permanent "did this arrive this turn" signal at all. New `ManaAbility.activationRestriction: { enteredThisTurn, orControlsBasicLand? }`, parsed in `parseManaInstruction` alongside the existing `requiresLands` restriction, enforced in `canUseManaAbility` (checked against `supertypes.includes("Basic")` for the basic-land clause). Fully implements Hidden Lair, Dark Fortress, Training Compound, Gleaming Bastion, Gathering Place, Mirrex (Mirrex's third ability — a Phyrexian Mite token — was already independently modeled). LIMIT: the same restriction phrase also appears on at least one non-mana activated ability (Fungus Elemental's counter ability); this claim only wires it for `ManaAbility`, not `ActivatedAbility` | Ready for integrator review |
| `rules-noncreature-spell-cast-each-player-drain` | `worker-05` | New `TRIGGER_TEMPLATES` entry: "Whenever a player casts a noncreature spell, ~" (`event: "spell-cast"`, `subject: "each-player"`, `spellType: "noncreature"`) — the `noncreature` filter and the `each-player` subject both already existed independently (used together for "you"/"opponent" elsewhere) but had no template combining them. Also widened the internal `TriggerTemplate` type's `spellType` union (it was missing `"noncreature"`, unlike the sibling `TriggerDefinition` type). No new effect needed — `"they lose N life"` already resolves via `lose-life-event-player` off the spell-cast event's own `controller` field, already captured into `eventController`. Fully implements Mai, Scornful Striker; Ruric Thar, the Unbowed; Medusa, Inhuman Queen | Ready for integrator review |
| `rules-surveil` | `worker-05` | New keyword action: Surveil N (CR 701.42) — look at the top N cards of your library, put any number into the graveyard, the rest on top in any order. Built by generalizing the existing Scry primitive rather than duplicating it: the `"scry"` `PendingChoice`/`beginScry` gained a `destination: "library-bottom" \| "graveyard"` field (defaulting to `"library-bottom"`, so every existing Scry call site and test is untouched), and the resolution step in `applyChooseScry` routes declined cards to `graveyard` instead of the library when `destination` is `"graveyard"`. New `SpellEffect` kind `"surveil"` wired at both the spell-effect and triggered-ability dispatch points (mirroring Scry's own two dispatch sites), plus `recognizeSentence` patterns for `"Surveil N"`. Fully implements 73 cards across the catalog (Surveil appears on 210; the rest have another unimplemented clause), including the target card Otherworldly Gaze plus Consider, Sinister Sabotage, Doom Whisperer, Unexplained Disappearance, Think Tank, Curate | Ready for integrator review |
| `rules-reanimate` | `worker-05` | New `recognizeSentence` patterns for the "Put target creature card from a graveyard onto the battlefield under your control" phrasing (Reanimate, Hymn of Rebirth) — distinct from the already-modeled "Return ... to the battlefield" wording. The plain form reuses the existing `return-target-creature-card-from-graveyard-to-battlefield` executor unchanged (it already moves the card under the caster's control regardless of whose graveyard it started in, via `putOntoBattlefield(next, object.controller, ...)`); the costed form ("You lose life equal to that card's mana value") is a new compound `reanimate-target-creature-lose-mana-value-life` effect that reads the card's mana value before moving it, then applies the life loss to the caster via `loseLife`. New `TargetKind` value used: `creature-card-in-a-graveyard` (already existed, just unused by any recognizer). Fully implements Reanimate, Hymn of Rebirth, plus (via existing trigger/optional-cost infrastructure with zero further changes) Teneb, the Harvester and Debtors' Knell | Ready for integrator review |
| `rules-keyword-during-your-turn` | `worker-05` | New static primitive: "~ has KEYWORD during your turn" (Razorkin Needlehead) — self-only and gated on whose turn it is, distinct from the existing `staticKeywordGrants` (which grants to other permanents, unconditionally). New `CardProfile.keywordsDuringYourTurn: readonly EnforcedKeyword[]`, parsed with the same `GRANTABLE_KEYWORDS`/`parseKeywordList` helpers `parseStaticKeywordGrant` already uses, and checked in `keywordOf` (`engine.ts`) against `state.activeSeat === permanent.controller`. Fully implements Razorkin Needlehead. LIMIT: Hunter's Blowgun uses the same clause but with an "otherwise it has reach" else-branch on an *equipped* creature, not modeled by this claim | Ready for integrator review |
| `rules-python-sacrifice-operands` | Preserve typed sacrifice operands and reusable primitive clusters in the Oracle compiler | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `rules-python-trigger-subjects` | Preserve reusable trigger subjects such as permanent-you-control in the Oracle compiler | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `rules-python-activated-cost-operands` | Preserve reusable discard, exile, and sacrifice cost operands for activated-ability batches | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `rules-python-graveyard-return-operands` | Preserve permanent-card graveyard return destinations for compiler clusters | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `tools-primitive-roadmap` | `tools/rules/plan_primitive_roadmap.py` and its unit tests: rank Oracle primitives by cards actually finished | `codex-ready/c14-combat-restrictions` | ready to integrate | 2026-09-03 |
| `tools-parallel-workers` | `tools/rules/plan_primitive_workers.py`: assign disjoint primitives to bounded workers and split 20-card commit batches | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c14-combat-restrictions` | Printed can't attack / can't block / attacks each combat if able / can block only creatures with X | `codex-ready/c14-combat-restrictions` | ready to integrate | 2026-09-03 |
| `c14-landwalk` | Landwalk evasion for every basic land type plus legendary landwalk | `codex-ready/c14-combat-restrictions` | ready to integrate | 2026-09-03 |
| `c13-activated-pump` | Activated self-creature P/T bonuses through end of turn | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-unblockable` | Printed creature can't-be-blocked restriction | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-activated-sacrifice-creature` | Activated costs that sacrifice a creature, including “another” | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-activated-sacrifice-artifact` | Activated costs that sacrifice an artifact, including “another” (CR 117.3b, 602.2b) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-activated-sacrifice-enchantment` | Activated costs that sacrifice an enchantment, including “another” (CR 117.3b, 602.2b) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-activated-sacrifice-land` | Activated costs that sacrifice a land, including “another” (CR 117.3b, 602.2b) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-activated-sacrifice-noncreature` | Activated costs that sacrifice any noncreature permanent (CR 117.3b, 602.2b) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-activated-sacrifice-permanent` | Activated costs that sacrifice a permanent, including “another” (CR 117.3b, 602.2b) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-activated-discard-cost` | Activated costs that discard one card from the controller’s hand (CR 117.3b, 602.2b) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-activated-sacrifice-token` | Activated costs that sacrifice a token permanent (CR 117.3b, 602.2b) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-activated-exile-graveyard-cost` | Activated costs that exile one card from the controller’s graveyard (CR 117.3b, 602.2b) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-graveyard-to-library-bottom` | Return a targeted card from your graveyard to the bottom of your library (CR 400.7, 701.19) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-graveyard-shuffle-into-library` | Shuffle a targeted card from your graveyard into your library (CR 400.7, 701.20) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-return-permanent-card-to-battlefield` | Return a targeted permanent card from your graveyard to the battlefield (CR 400.7, 603.3) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-return-permanent-card-from-any-graveyard` | Return a targeted permanent card from any graveyard to the battlefield (CR 400.7, 603.3) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-exile-permanent-card-from-graveyard` | Exile a targeted permanent card from your graveyard (CR 400.7, 701.11) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-exile-permanent-card-from-any-graveyard` | Exile a targeted permanent card from any graveyard (CR 400.7, 701.11) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c17-when-enters-return-target` | Archaeomancer and Izzet Chronarch return a target instant or sorcery card from your graveyard to your hand (CR 109.2a, 400.7, 603.2, 603.3d, 608.2b-c) | `feat/activated-abilities-and-triggers` | active | 2026-09-04 |
| `arsenal-loyal-retainers` | Loyal Retainers: precombat activated sacrifice and legendary-creature graveyard return (CR 602.2b, 608.2b) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `arsenal-miraris-wake-pt` | Mirari’s Wake static +1/+1 grant to creatures you control (CR 613.4, 613.5) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `arsenal-miraris-wake-mana` | Mirari’s Wake adds one mana of a produced type when a controlled land produces mana (CR 605.1, 613.6) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `arsenal-command-tower` | Command Tower produces mana restricted to the commander’s color identity (CR 903.5d, 106.1) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `arsenal-chaos-warp` | Chaos Warp shuffles a target permanent into its owner’s library and conditionally puts the revealed top card onto the battlefield (CR 701.20, 701.34) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `arsenal-decree-of-pain` | Decree of Pain destroys all creatures/draws for creatures destroyed and its cycling -2/-2 trigger (CR 603.2, 608.2c, 701.7, 702.29) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `arsenal-desertion` | Desertion counters a spell and puts an artifact or creature spell onto the battlefield under its controller’s control | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `arsenal-maelstrom-haste` | Reuse the static “creatures you control have haste” primitive for Maelstrom Wanderer; cascade remains separate | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `arsenal-vela-intimidate` | Intimidate keyword/static grants plus Vela’s leaves-the-battlefield trigger (CR 603.6c, 702.13, 509.1a) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-each-player-spell-trigger` | Triggered abilities that watch any player cast a spell (CR 603.2, 603.3) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-permanent-etb-trigger-subject` | ETB triggers for any permanent entering under the controller’s control (CR 603.2) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-then-you-have-more` | Sequential life comparison followed by conditional self-draw, reusable for Survival Cache (CR 608.2c) | `codex/c13-life-comparison-f99` | active | 2026-09-04 |
| `c13-when-enters-return-target` | Optional ETB artifact recovery with life gain equal to the returned card's mana value, applied to Razor Hippogriff (CR 603.2, 608.2) | `codex/c13-hippogriff-reclaim-f99` | active | 2026-09-04 |
| `c13-when-you-cycle-you` | Optional cycle trigger with reusable all-creature damage, applied to Slice and Dice (CR 603.2, 702.29) | `codex/c13-cycle-may-have-f99` | merged (`7be4ca9`) | 2026-09-04 |
| `c13-when-you-cycle-you-2` | Optional cycle trigger with targeted temporary fear, applied to Dirge of Dread (CR 603.2, 702.29) | `codex/c13-cycle-may-have-f99` | merged (`7be4ca9`) | 2026-09-04 |
| `c13-activated-remove-counters` | Activated costs that remove counters from the source permanent | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-global-temporary-keyword` | Temporary keyword grants to all permanents controlled by the caster | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-life-equals-power` | Gain life equal to the current power of a targeted creature you control | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-compound-draw-life-loss` | Compound draw-and-life-loss effects used by upkeep triggers | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-damage-equal-hand` | Damage to a player equal to cards in that player's hand | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-static-haste-grant` | Static “creatures you control have haste” grants | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-draw-step-additional-card` | Draw-step triggers that draw an additional card for the active player | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-opponent-hand-minus-damage` | Opponent-upkeep damage equal to active player's hand count minus an offset | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-conditional-subtype-trigger` | Conditional triggers gated by controlling no permanent of a subtype | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-draw-tapped-creatures` | Draw a card for each tapped creature controlled by a targeted opponent | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-global-creature-keyword` | Temporary keyword grants to all creatures on the battlefield | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-life-gain-prevention` | Static prevention of all player life gain | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-no-maximum-hand-size` | Static removal of the cleanup maximum-hand-size discard | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-static-creature-pt-grant` | Static +P/+T bonuses for other creatures you control | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-destroy-power-toughness-loss` | Destroy a creature then make its controller lose its power plus toughness | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-variable-global-debuff` | X-scaled -X/-X effects affecting every creature | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-conditional-power-trigger` | Optional triggers gated by a controlled creature's power threshold | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-nonflying-global-damage` | X-scaled damage to nonflying creatures and each player | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-upkeep-draw-life-loss` | Upkeep draw-and-life-loss compound trigger wording | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-fear-evasion` | Fear keyword and black-or-artifact blocking restriction | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-equal-hand-upkeep-damage` | Opponent-upkeep damage equal to active player's hand size | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-counted-artifact-life` | Life gain multiplied by controlled artifacts | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-named-source-counters` | Named non-power counters placed on a triggered source | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-global-fear-test` | Scenario coverage for global fear grants | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-counted-life-types` | Reusable counted life gain for artifacts, creatures and enchantments | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-counted-creature-tokens` | Token creation scaled by controlled creature count | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-power-threshold-targets` | Legal target filtering for creatures with power five or greater | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-continuous-flying-targets` | Continuous flying grants respected by flying-target filters | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-other-creature-keyword-grants` | Static keyword grants excluding their source creature | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-fear-token-keywords` | Preserve fear when parsing generated token keywords | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-equal-hand-life-loss` | Life loss equal to each player's hand size | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-wheel-discard-draw` | Deterministic discard-hand then draw count effect | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-targeted-hand-discard` | Move a targeted player's full hand to graveyard | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-counted-target-life-loss` | Targeted life loss scaled by controlled type count | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-mill-each-player` | Mill a fixed or X amount from every library | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-player-only-global-damage` | Damage every player without affecting permanents | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-counted-land-life` | Count controlled lands in scalable life gain | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-life-per-permanent` | Life gain scaled by all controlled permanents | `codex/c13-equipment-cluster` | active | 2026-09-03 |
| `c13-draw-controlled-type` | Draw one card per controlled creature, artifact, enchantment or land | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-counted-any-target-damage` | Any-target damage scaled by a controlled type count | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-toughness-threshold-targets` | Creature targets filtered by toughness four or greater | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-low-power-targets` | Creature targets filtered by power four or less | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-low-toughness-targets` | Creature targets filtered by toughness four or less | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-defender-targets` | Creature targets filtered by enforced defender | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-deathtouch-targets` | Creature targets filtered by enforced deathtouch | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-lifelink-targets` | Creature targets filtered by enforced lifelink | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-menace-targets` | Creature targets filtered by enforced menace | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-haste-targets` | Creature targets filtered by enforced haste | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-first-strike-targets` | Creature targets filtered by enforced first strike | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-double-strike-targets` | Creature targets filtered by enforced double strike | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-trample-targets` | Creature targets filtered by enforced trample | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-vigilance-targets` | Creature targets filtered by enforced vigilance | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-indestructible-targets` | Creature targets filtered by enforced indestructible | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-hexproof-targets` | Creature targets filtered by enforced hexproof and ownership | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-shroud-targets` | Shroud target prohibition kept in target filtering | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-reach-targets` | Creature targets filtered by enforced reach | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-flying-only-sweeper` | Damage effects restricted to creatures with flying | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-static-keyword-gain-verb` | Alternate gain wording for static keyword grants | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-global-static-keywords` | Static keyword grants affecting every creature | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-global-static-pt` | Global static power/toughness layer bonuses | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-global-static-pt-dedup` | Prevent duplicate application of global static bonuses | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-counted-planeswalkers` | Planeswalkers accepted by controlled-type scaling | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-counted-battles` | Battles accepted by controlled-type scaling | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-return-artifact-to-hand` | Return target artifact permanents to their owners' hands | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-return-enchantment-to-hand` | Return target enchantment permanents to their owners' hands | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-return-enchantment-from-graveyard` | Return target enchantment card from graveyard to battlefield | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-exile-card-from-any-graveyard` | Exile target card from any graveyard | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-return-card-from-any-graveyard` | Return target card from any graveyard to its owner's hand | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-exile-creature-from-any-graveyard` | Exile target creature card from any graveyard | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-exile-artifact-from-any-graveyard` | Exile target artifact card from any graveyard | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-exile-enchantment-from-any-graveyard` | Exile target enchantment card from any graveyard | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-exile-land-from-any-graveyard` | Exile target land card from any graveyard | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-artifact-etb-trigger-subject` | Trigger subjects for artifacts entering under your control | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-enchantment-etb-trigger-subject` | Trigger subjects for enchantments entering under your control | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-storage-counter-mana-output` | Consume variable storage-counter mana lines already executed by the mana engine (Saltcrusted Steppe, Molten Slagheap; CR 605.1a) | `feat/activated-abilities-and-triggers` | active | 2026-09-04 |
| `c13-reveal-top-gain-mana-value` | Augury Adept top-card reveal, move to hand and mana-value life gain | `codex/c13-augury-adept` | active | 2026-09-04 |
| `c13-reveal-until-creature` | Foster reveal until a creature, move it to hand and revealed cards to graveyard | `codex/c13-foster` | active | 2026-09-04 |
| `c13-threshold-graveyard-return` | Stitch Together threshold branch between battlefield and hand | `codex/c13-stitch-together` | active | 2026-09-04 |
| `c13-choose-both` | Soul Manipulation and Fissure Vent modal one-or-both selection | `codex/c13-choose-both` | active | 2026-09-04 |
| `c13-choose-more` | Parameterized modal subsets for `Choose N or more`, including ordered target slots for Rain of Thorns (CR 700.2, 601.2b) | `codex/c13-choose-more-a32` | active | 2026-09-04 |
| `c13-graft` | Reusable Graft entry counters and optional counter transfer to another entering creature (CR 702.58, 122.1, 603.2) | `codex/c13-graft-f99` | active | 2026-09-04 |
| `c13-whenever-creature-dies-untap` | Reusable death-triggered source untap (CR 603.2, 701.21) | `codex/c13-goblin-sharpshooter-f99` | active | 2026-09-04 |
| `c13-whenever-creature-you-control` | Reusable entering-creature power damage to any target (CR 603.2, 120.2, 603.3d) | `codex/c13-warstorm-surge-f99` | active | 2026-09-04 |
| `c13-whenever-creature-you-control-2` | Power-threshold entering-creature trigger with optional targeted damage (CR 603.2, 603.4, 120.2) | `codex/c13-where-ancients-tread-f99` | active | 2026-09-04 |
| `c13-oracle-damage-triggered-shape-4` | Any-player draw trigger that deals damage to the drawing player (CR 603.2, 603.3d, 120.2) | `codex/c13-spiteful-visions-f99` | active | 2026-09-04 |
| `c13-oracle-damage-triggered-shape-5` | Optional attack trigger that taps a chosen number of untapped creatures of a subtype, then pumps and damages the attacked player (CR 508.1i, 601.2) | `codex/c13-myr-battlesphere-f99` | active | 2026-09-04 |
| `c13-oracle-gain-life-triggered-5` | Life-gain event carries its amount into a targeted opponent life-loss trigger (CR 603.2, 120.3) | `codex/c13-sanguine-bond-f99` | active | 2026-09-04 |
| `c13-oracle-other-activated-shape-2` | Activated global shroud grant for creatures you control until end of turn (CR 113.6, 702.18) | `codex/c13-aerie-mystics-f99` | active | 2026-09-04 |
| `c13-oracle-other-activated-shape-4` | Activated target power threshold grant: target creature with power 5 or greater gains first strike until end of turn (CR 601.2c, 702.7) | `codex/c13-rakeclaw-f99` | active | 2026-09-04 |
| `c13-oracle-other-activated-shape-3` | Activated creature-only control reset: each player gains control of all creatures they own (CR 110.2,  control-change effects) | `codex/c13-homeward-path-f99` | active | 2026-09-04 |
| `c13-oracle-other-activated-shape-6` | Activated temporary animation: source becomes a 2/2 white and blue Bird artifact creature with flying until end of turn (CR 613.6, 707.2) | `codex/c13-azorius-keyrune-f99` | active | 2026-09-04 |
| `c13-was-kicked-has-split` | Conditional Split second static keyword while a spell is kicked, reusable for Molten Disaster (CR 702.33e, 702.61) | `codex/c13-kicked-split-ebc` | active | 2026-09-04 |
| `c13-oracle-damage-triggered-shape-6` | Combat-damage trigger draws the damage amount for both the source controller and damaged player (CR 510.2, 603.2) | `codex/c13-diviner-spirit-f99` | merged (`5648bad`) | 2026-09-04 |
| `c13-oracle-create-token-triggered` | Another nontoken creature you control dying creates a 3/1 black-red Graveborn token with haste (CR 603.2, 603.6d) | `codex/c13-sekkuar-f99` | merged (`5648bad`) | 2026-09-04 |

| `c13-oracle-counter-activated-cost` | Opal Palace commander-identity mana ability marks mana used for a commander cast and applies its cast-count +1/+1 counters (CR 605.1a, 614.1c, 903.8) | `codex/c13-mana-priority-f99` | merged (`c595ea7`) | 2026-09-04 |
| `c13-oracle-damage-activated-cost` | Leonin Bladetrap sacrifices itself to deal damage to attacking creatures without flying (CR 602.2b, 608.2c, 120.4) | `codex/c13-leonin-bladetrap-f99` | merged (`8b18b44`) | 2026-09-04 |
| `c13-oracle-damage-triggered-shape-7` | Fell Shepherd returns all creature cards put into your graveyard from the battlefield this turn after combat damage to a player (CR 603.2, 603.3d, 400.7) | `codex/c13-fell-shepherd-f99` | merged (`054deab`) | 2026-09-04 |
| `c13-oracle-damage-triggered-target` | Stalking Vengeance uses a dead creature's last-known power to damage a target player or planeswalker (CR 603.2, 603.3d, 109.5) | `codex/c13-stalking-vengeance-f99` | merged (`15f50ee`) | 2026-09-04 |
| `c13-oracle-destroy-activated-target` | Deepfire Elemental pays {X}{X}{1} and destroys a target artifact or creature with mana value X (CR 602.2b, 107.3, 608.2b) | `codex/c13-deepfire-elemental-f99` | merged (`0e824f9`) | 2026-09-04 |
## Claim format

Copy this row when starting work:

```text
| `cluster-or-batch` | exact primitive, files and/or Scryfall IDs | branch / PR | active | YYYY-MM-DD |
```

Use one row per independent worker. Keep a claim active until the PR is merged
or explicitly abandoned. After merge, change it to `merged` and record the
commit in the handoff. If a worker discovers that its scope is larger than
declared, it must update the claim before touching the additional files.

Claims coordinate contributors but do not replace review: CI, scenario tests,
rules citations and the integrator review remain mandatory.

Integration note (2026-09-06): worker-05 currently contains useful rules
commits based on an incompatible rewritten tree. Do not merge that branch as a
whole. Resubmit from the published integration SHA with a focused diff; the
integrator may rescue individual primitives only after the diff gate and
scenario audit pass.

## Integrator directive (2026-09-06)

Commander 2013 is the current completion push. Workers may continue taking
unclaimed C13 clusters until its coverage is closed, but must claim a
disjoint cluster first and must extract reusable primitives instead of adding
card-specific duplicates. The integrator reviews incoming commits in batches,
keeps only tested changes, and records every accepted card as `name | oracle_id`.
No worker may reuse an active claim or treat a C13 claim as permission to edit
the shared gameplay engine without declaring that primitive explicitly.
| `c13-aura-control-enchanted-creature` | Control Magic's continuous control effect for its enchanted creature, with restoration when the Aura leaves (CR 110.2, 303.4, 613.7) | `codex/c13-one-line-7bd` | merged (`d1d8e792`) | 2026-09-05 |
| `c13-aura-granted-activation` | Enchanted creatures or lands receive a reusable quoted activated ability from the Aura (CR 113.6, 303.4, 605.1a) | `codex/c13-aura-granted-activation-7bd` | merged (`53206780`) | 2026-09-05 |
| `arsenal-edric-combat-draw` | Edric draws optionally when a creature deals combat damage to one of his opponents (CR 603.2, 603.5, 120.3) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `arsenal-minds-eye` | Mind's Eye watches opponent card draws and supports optional payment of {1} before drawing (CR 603.2, 603.5, 121.1) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `arsenal-duplicant-etb-imprint` | Duplicant optionally exiles a target nontoken creature on ETB and copies its power/toughness through last-known imprint (CR 603.2, 603.5, 607.2, 707.2) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `arsenal-rhystic-study` | Rhystic Study asks the exact spell caster to pay {1}; if they decline, its controller draws a card, with no pay action exposed when the cost is unaffordable (CR 603.2, 603.5, 117.1, 121.1) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-boros-charm-modal-self-reference` | Boros Charm's three modes plus normalization of printed card-name self references (CR 601.2b, 700.2) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `azorius-charm-library-top` | Azorius Charm's creature-to-owner-library-top mode, draw mode, and controlled-creature pump mode; shared card primitive (CR 601.2b, 701.18) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-naya-charm-modal` | Naya Charm's three modes, including damage to a creature and tapping all creatures controlled by a target player (CR 601.2b, 609.3, 701.21) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-gahiji-attack-buff` | Gahiji's attack trigger buffs the attacking creature only when it attacks one of its controller's opponents (CR 603.2, 508.1i, 613.4) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-guttersnipe-spell-trigger` | Guttersnipe triggers from your instant/sorcery casts and deals 2 damage to each opponent (CR 603.2, 603.5, 608.2c) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-fecundity-death-draw` | Fecundity lets the controller of each creature that dies optionally draw a card (CR 603.2, 603.5, 603.6a, 121.1) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-fires-yavimaya-activation` | Fires of Yavimaya grants haste and sacrifices itself for a controlled-creature +2/+2 activation (CR 602.2b, 613.4, 611.3) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-goblin-bombardment-activation` | Goblin Bombardment sacrifices a creature as a cost to deal 1 damage to any target (CR 602.2b, 117.1) | `codex/c13-equipment-cluster` | active | 2026-09-04 |
| `c13-krosan-warchief-beast-reduction` | Static reduction for Beast spells you cast, reusable subtype-aware cost modifier (CR 118.9) | `codex/c13-krosan-warchief-a32` | active | 2026-09-04 |
| `c13-charmbreaker-random-spell-recovery` | Beginning-of-upkeep recovery of a random instant or sorcery card from your graveyard to your hand | `codex/c13-charmbreaker-devils` | active | 2026-09-04 |
| `c13-conjurers-closet-blink` | Optional end-step exile and return of a target creature you control, reusable blink primitive for Conjurer's Closet (CR 603.2, 603.5, 610.3, 400.7) | `codex/c13-conjurers-closet` | active | 2026-09-04 |
| `c13-tidal-force-tap-untap-choice` | Optional each-upkeep choice to tap or untap a target permanent for Tidal Force (CR 603.2, 603.5, 701.21) | `codex/c13-tidal-force` | active | 2026-09-04 |
| `c13-echo-cost` | Reusable Echo cost and next-upkeep pay-or-sacrifice handling (CR 702.30) | `feat/activated-abilities-and-triggers` | merged (this commit) | 2026-09-04 |
| `c13-thunderstaff-combat-prevention` | Thunderstaff prevents 1 combat damage from creatures to its controller while untapped (CR 615.1, 614.1) | `feat/activated-abilities-and-triggers` | merged (this commit) | 2026-09-04 |
| `c13-blue-spells-and-red` | Nightscape Familiar's shared reduction for blue or red spells you cast (CR 118.9) | `feat/activated-abilities-and-triggers` | merged (this commit) | 2026-09-04 |
| `c13-the-beginning-each-end` | Brooding Saurian's end-step owner-control reset | `codex/c13-brooding-saurian` | merged (`ebc7243`) | 2026-09-04 |
| `c13-the-beginning-your-end` | Wall of Reverence's optional end-step life gain | `codex/c13-wall-reverence` | active | 2026-09-04 |
| `c13-the-beginning-your-upkeep` | Capricious Efreet's upkeep random destruction choice | `codex/c13-capricious-efreet` | review (`a868c8d`) | 2026-09-04 |
| `c13-beast-spells-you-cast` | Krosan Warchief's Beast spell cost reduction | `codex/c13-krosan-warchief-a32` | review (`d755782`) | 2026-09-04 |
| `c13-choose-more` | Rain of Thorns choose-N-or-more modal selection | `codex/c13-choose-more-a32` | review (`b6ba4d1`) | 2026-09-04 |
| `c13-players-have-maximum-hand` | Price of Knowledge global maximum-hand-size replacement | `codex/c13-global-hand-limit-a32` | review (`bc09726`) | 2026-09-04 |
| `c13-oracle-counter-activated-target` | Vile Requiem's verse-counter upkeep and self-sacrifice destruction scaled by its counters (CR 602.2b, 603.2b, 121.1) | `codex/c13-vile-requiem` | merged (`0d82d42`) | 2026-09-04 |
| `c13-oracle-draw-triggered-shape` | Well of Lost Dreams variable optional payment capped by life gained, then draw X (CR 603.2, 603.5, 107.3, 118.3) | `codex/c13-well-lost-dreams` | merged (`0d74880`) | 2026-09-04 |
| `c13-oracle-draw-activated-cost-2` | Jace's Archivist: each player discards their hand, then all draw the greatest discarded hand size; reusable shared-discard primitive (CR 602.2b, 701.8, 121.1) | `codex/c13-f99-b033-next` | active | 2026-09-04 |




| `c13-brooding-saurian-owner-control` | Each-end-step control reset for all nontoken permanents to their owners (CR 603.2, 603.6, 110.2) | `codex/c13-brooding-saurian` | merged (`ebc7243`) | 2026-09-04 |
| `c13-serene-master` | Serene Master exchanges its power with the creature it blocks until end of combat (CR 603.2, 603.3d, 613.4, 701.10) | `codex/c13-serene-master-f99` | merged (`f0d1a33`) | 2026-09-05 |
| `c13-mirari-copy` | Mirari optionally pays {3} to copy your instant or sorcery spell while preserving its targets (CR 603.2, 603.3d, 707.10, 707.12) | `codex/c13-mirari-f99` | active | 2026-09-05 |
| `rules-second-draw-this-turn` | Faerie Mastermind's "draws their second card each turn" trigger condition, gated on a new per-turn draw counter rather than any draw (CR 603.2, 603.3, 121.1) | `worker-05` | active | 2026-09-05 |
| `rules-draw-then-put-back-on-top` | Brainstorm's "draw N, then put M cards from your hand on top of your library in any order," generalizing the existing single-card `hand-card-to-library-top` choice into an iterative multi-pick (CR 601.2h, 701.8) | `worker-05` | active | 2026-09-05 |
| `rules-discard-target-player-then-draw-same` | Forget's "target player discards two, then draws as many cards as they discarded this way," attaching an optional draw-back to the existing `discard-cards` choice; also removed a latent duplicate `discard-cards` union member left over from an earlier merge (CR 701.8) | `worker-05` | active | 2026-09-05 |
| `rules-put-active-player-hand-on-library-bottom-then-draw-same` | Teferi's Puzzle Box's "each player's draw step: put hand on bottom in any order, then draw that many," reusing the existing draw-step/each-player trigger template and the active-player resolution pattern already established by Draw Mine (CR 504.1, 701.8) | `worker-05` | active | 2026-09-05 |
| `rules-source-untapped-condition` | Howling Mine's "if ~ is untapped" trigger gate, a new `source-untapped` `TriggerDefinition.condition` checking the watching permanent's own tapped state, reusing Draw Mine's existing `draw-active-player` effect for the actual draw (CR 603.4) | `worker-05` | active | 2026-09-05 |
| `rules-draw-half-library-then-lose-half-life-target-player` | Peer into the Abyss's "draws cards equal to half library, loses half life, round up each time," plus a new reusable `isIgnorableSentence` entry for "Round up each time." as a rounding clarifier rather than a separate action (CR 107.1a) | `worker-05` | active | 2026-09-05 |
| `rules-fevered-visions` | Fevered Visions' each-player end-step draw plus opponent-only conditional damage gated on post-draw hand size; added the missing "each player's end step" `TRIGGER_TEMPLATES` entry (CR 603.2, 603.3) | `worker-05` | active | 2026-09-05 |
| `rules-discarded-card-type-filter` | Waste Not's three type-filtered discard triggers (creature/land/noncreature-nonland), a new `discardedCardType` field on `TriggerTemplate`/`TriggerDefinition` mirroring the existing `spellType` filter, reusing `create-token`/`add-mana`/`draw` for the three reactions (CR 603.2, 701.8) | `worker-05` | active | 2026-09-05 |
| `rules-each-player-draws-then-discards` | Geier Reach Sanitarium's "each player draws a card, then discards a card," a new `nextSeats` chain on the existing `discard-cards` choice queuing each player's own discard in APNAP order after a shared unconditional draw (CR 101.4, 701.8a) | `worker-05` | active | 2026-09-05 |
| `rules-free-cast-if-commander` | Deadly Rollick's "if you control a commander, you may cast this spell without paying its mana cost," a new `freeCastIfCommander` alternative-cost primitive threaded through `castableCard`/`applyCast`/`legalActions` as an option offered alongside the normal paid cast, not instead of it; also flips Flawless Maneuver and Fierce Guardianship (CR 601.2b, 118.9) | `worker-05` | active | 2026-09-05 |
| `rules-pay-life-instead-of-mana-cost` | Snuff Out's "if you control a Swamp, you may pay 4 life rather than pay this spell's mana cost," a second alternative-cost shape (`payLifeInsteadOfManaCost`) sharing the same `castableCard`/`applyCast`/`legalActions` plumbing as the free-cast primitive (CR 601.2b, 118.9) | `worker-05` | active | 2026-09-05 |
| `rules-counter-target-spell-unless-pay` | Daze's "counter target spell unless its controller pays {1}," a new `counter-target-spell-unless-pay` effect that reuses the existing `optional-trigger`/`unlessPayCost` pendingChoice machinery for the target spell's own controller to decide; plus a third alternative-cost shape (`returnLandInsteadOfManaCost`, return a land instead of paying). Flips ~35 cards sharing the same counter-unless-pay phrasing, including Mana Leak (CR 601.2b, 603.3, 118.9) | `worker-05` | active | 2026-09-05 |
| `rules-delayed-mana-next-main-phase` | Mana Drain's "at the beginning of your next main phase, add mana equal to that spell's mana value," a new `DelayedManaAdd` queue (seat-keyed, resolved at `precombat-main`) mirroring the existing `DelayedDraw` mechanism used for Arcane Denial (CR 603.7) | `worker-05` | active | 2026-09-05 |
| `rules-draw-caused-trigger-doubler` | Krang, the All-Powerful's "if a player drawing a card causes a triggered ability of a permanent you control to trigger, that ability triggers an additional time," a new `draw-caused-triggers` scope on the existing `TriggerDoubler` primitive, keyed on the causing event's kind rather than the watching permanent's subtype (CR 603.3f) | `worker-05` | active | 2026-09-05 |
| `rules-pay-reduced-cost-instead` | Baleful Mastery's "you may pay {1}{B} rather than pay this spell's mana cost. If the {1}{B} cost was paid, an opponent draws a card," a fourth alternative-cost shape (`payReducedCostInstead`) plus a new `StackObject.castViaAlternativeCost` flag threaded through `pushOnStack` so a resolving effect can read how it was cast; also a new `creature-or-planeswalker` `TargetKind` (CR 601.2b, 601.2f) | `worker-05` | active | 2026-09-05 |
| `rules-gift-keyword` | Long River's Pull's Gift keyword: promising the gift at cast time widens the legal target set (`CardProfile.giftPromisedTargetKind`) for the same printed effect and draws the gifted opponent a card before the spell's other effects (`giftDrawsCard`), offered as a same-cost cast variant alongside the normal one (CR 702.166) | `worker-05` | active | 2026-09-05 |
| `rules-attack-tax-per-creature` | Propaganda's "creatures can't attack you unless their controller pays {2} for each creature they control that's attacking you," a new `attackTaxPerCreature` static ability enforced (and paid) in `applyDeclareAttackers`, with `legalActions` pre-filtering unaffordable single-attacker declarations so a bot can never pick an action that would throw (CR 508.1a) | `worker-05` | active | 2026-09-05 |
| `rules-orcish-bowmasters` | Orcish Bowmasters' "except the first [draw] in each of their draw steps" trigger condition (new `PlayerState.drawsThisDrawStep` counter, reset at draw-step begin) plus the Amass mechanic (`amass` effect: grow a controlled Army or create one), combined via a new "enters and whenever X" compound-trigger template and a generic "X. Then amass Type N" sentence combinator (CR 603.2, 701.44) | `worker-05` | active | 2026-09-05 |
| `rules-mjolnir-hammer-of-thor` | Mjölnir, Hammer of Thor's three clauses: (1) "deals 4 damage to up to one target creature" reuses the existing multi-target `targetKinds`/`minimumTargets` machinery with `minimumTargets: 0` — a zero-plumbing discovery that "up to one target" is "0-or-more from a 1-element array"; (2) "Equip worthy" (new `equipWorthyCost` field plus an `isWorthyCreature`/`equipTargets` predicate restricting Equip's target to a legendary, non-Villain, red-and/or-white creature) and "Double all damage equipped creature would deal" (`doublesEquippedCreatureDamage`, a multiplicative `equippedCreatureDamageMultiplier` wired into all 3 damage-dealing call sites: `dealDamageFromObject`, `dealDamageToPermanent`, and `applyCombatDamage`'s to-players loop); (3) "{2}{R}, Discard this card: deals 2 damage to each creature," a new `discardsSelf` activation-cost primitive (forces `sourceZone: "hand"`, pays by moving its own source card to the graveyard before the ability resolves) (CR 301.5c, 603.2, 601.2c) | `worker-05` | active | 2026-09-05 |
| `rules-class-mechanic-wizard-class` | New Class enchantment subsystem (CR 702.134), first landed via Wizard Class: `Permanent.classLevel`, `CardProfile.classLevels` (`{cost}: Level N` lines parsed into synthetic per-level `ActivatedAbility`s gated by a new `requiresClassLevel`), a new `class-level-up` `TriggerEvent`/`SpellEffect` pair, and a `TriggerDefinition.minClassLevel` gate for ability lines that only activate once a level is reached — derived by a position-based `classLevelByLine` pass over the raw oracle text (since `recognizeText` itself is card-type-agnostic), applied as a post-process over its returned triggers rather than by threading level context through the parser. "When this Class becomes level N, X" is self-gating (a new `class-level-reached` condition keyed on the event's own level) and needed no positional pass at all. Also added the generic "Put a/an/N +1/+1 counter(s) on target creature you control" effect pattern (missing until now; flips other, non-Class cards too) (CR 702.134c/d) | `worker-05` | active | 2026-09-05 |
| `rules-black-market-connections` | Black Market Connections' "At the beginning of your first main phase, choose one or more —" needed two new primitives: a `first-main-phase` `TriggerEvent` (raised alongside the existing precombat-main hook — this project has no "additional main phase" effects, so the two coincide for every in-scope card) and a `TriggerDefinition.modalEffects` field carrying every legal non-empty mode subset, resolved via a new `"trigger-mode"` `PendingChoice`/`choose-trigger-mode` action pair — the SAME timing model as trigger targets (CR 603.3d: chosen when the ability is put on the stack), not a spell's cast-time modal choice. Each subset's compound effect is applied by temporarily overriding the `TriggerInstance.definition.effect` before calling the existing `triggerStackObject`, so resolution itself needed no new code. Also added a bot heuristic (`botAction`) picking the richest subset by joined-label length, since this new choice type has no generic "pass" fallback and would otherwise stall a bot that draws this card. Caught mid-implementation: a first attempt at teaching `recognizeSentence` a generic "X. You lose N life." combinator collided with Read the Bones' existing "Scry 2, then draw two cards. You lose 2 life." (previously two independent top-level effects) by reshaping it into one nested compound — reverted and rebuilt as a LOCAL sentence-split-and-join step inside this card's own bullet parsing instead of a global grammar change (CR 603.3d, 505.1a) | `worker-05` | active | 2026-09-05 |
| `rules-notion-thief-draw-redirect` | Notion Thief's "If an opponent would draw a card except the first one they draw in each of their draw steps, instead that player skips that draw and you draw a card" is a genuine CR 614/616 replacement effect (not a trigger): it swaps WHO draws without revealing anything, so it never crosses the hidden-information boundary despite first looking like it might. Implemented as a check directly inside `drawCards`'s per-card loop (the same "except the first ... draw step" condition Orcish Bowmasters' trigger already tracks via `drawsThisDrawStep`) — when it applies and an opponent controls the new `redirectsOpponentDrawsExceptFirst` static, the affected player's draw is skipped outright (their library is untouched) and a normal recursive `drawCards` call draws for the Notion Thief controller instead. Also fixed a latent, previously-uncovered ordering bug this surfaced: `beginStep`'s mandatory per-turn draw ran BEFORE `drawsThisDrawStep` was reset to 0 for that same step, so each player's very first real draw step of the game (right after their 7-card opening hand, itself dealt through `drawCards` and so also incrementing this counter) was misread as "not the first draw of the draw step" — moved the reset immediately before the mandatory draw call. Caught while writing the scenario test for that exact "opponent's own draw-step draw is NOT redirected" case, which needed the fix to pass at all (CR 614.1, 616.1) | `worker-05` | active | 2026-09-05 |
| `rules-gitaxian-probe-view-hand` | Gitaxian Probe's "Look at target player's hand" DOES genuinely cross the hidden-information boundary — unlike Notion Thief, this one really does need one player to see another's cards — but it fits the SAME pattern `projection.ts` already uses for Scry/top-of-library review: a `PendingChoice` scoped to exactly one seat, whose private contents are computed in `projectGame` ONLY when `viewerSeat === choice.seat`. New `"view-hand"` `PendingChoice`/`ViewedHandView` pair: the target's hand is copied into the projection only for the entitled viewer (the caster), never for the target or any other seat, and a new `acknowledge-view-hand` action closes it (moving the spell to its owner's graveyard/exile then, exactly like Scry defers that move until its own choice concludes). "Draw a card" resolves BEFORE the view opens, mirroring Scry's "resolve every sibling effect first" ordering discipline. Also added a `botAction` heuristic to auto-acknowledge, since this choice type has no generic "pass" fallback either (CR 701.20, 616) | `worker-05` | active | 2026-09-05 |
| `rules-reforge-the-soul-miracle` | The Miracle keyword (CR 702.93), a new alternative-cost timing window: `CardProfile.miracleCost`, parsed the same way `evokeCost`/`echoCost` already are. Rather than threading through `castableCard`'s big always-available-from-hand alt-cost family (free-cast, pay-life, return-land, pay-reduced — all offered any time the card sits in hand), Miracle needed a genuinely different shape: it is offered exactly once, for exactly one card, in the single window right after `drawCards` adds it to hand as that player's first draw of the turn — so it is checked directly inside `drawCards`'s existing per-card loop (reusing `drawsThisTurn`, already tracked for Krang/Faerie Mastermind) rather than as a cast-time option. Opens a new `"miracle"` `PendingChoice` with a dedicated `cast-miracle`/`decline-miracle` action pair; casting pays the reduced cost directly and pushes the card onto the stack via the existing `pushOnStack` with `castViaAlternativeCost: true`, declining just leaves the card in hand for a normal cast later. `applyCastMiracle` is a small, self-contained cost-and-push function rather than a `castableCard`/`applyCast` extension, since Miracle's timing (an interstitial reveal window, not a hand-cast menu entry) doesn't fit that machinery's assumptions. Also added a `botAction` heuristic (pay if affordable) since, like the other new pause-and-choose primitives this session, it has no generic "pass" fallback (CR 702.93) | `worker-05` | active | 2026-09-05 |
| `c13-brooding-saurian-owner-control` | Each-end-step control reset for all nontoken permanents to their owners (CR 603.2, 603.6, 110.2) | `codex/c13-brooding-saurian` | active | 2026-09-04 |
| `c13-oracle-return-static-spell` | Wash Out chooses a color, then returns all permanents of that color to their owners' hands (CR 601.2b, 609.3, 701.19) | `codex/c13-wash-out-f99` | active | 2026-09-05 |
| `rules-naktamun-lorespinner-prepared` | Naktamun Lorespinner // Wheel of Fortune closed the deck's last card and turned out NOT to need a transform/DFC framework at all — "Prepared" (a brand-new mechanic) never changes the permanent's zone or characteristics; it only lets its controller "cast a copy of its [other face's] spell," a genuine CR 707.14-style copy that ceases to exist once it leaves the stack, which this engine's `fromCopy` flag already fully supports (added earlier for `copy-triggered-spell`). New pieces: a `become-prepared` `SpellEffect` and an `any-player-hand-at-most` trigger condition parse the front face's "At the beginning of your upkeep, if a player has N or fewer cards in hand, ~ becomes prepared."; a new `Permanent.prepared` flag; a new exported `backFace(card)` (mirroring the existing private `frontFace`, but keyed under a synthetic `${scryfall_id}::back` to avoid colliding with the front face's cached `cardProfile` entry) whose profile is computed — recursively, once, only for cards that actually have a `become-prepared` trigger — into `CardProfile.preparedCast` (cost/effect/targetKind/name lifted from the back face's own profile); and a `cast-prepared-copy` action/`applyCastPreparedCopy` that pays the back face's cost, flips `prepared` back off, and pushes a synthetic `GameCard` for the copy via `pushOnStack`'s now-parameterized `fromCopy` flag — needing zero new resolution code, since `resolveTop`/`sendSpellToOwnerZone` already special-case `fromCopy` to skip any zone move. Caught while writing the scenario test: `twoSeatGame` already settles all the way to turn 1's own precombat-main during `createGame`, before any test-added permanent exists — so a permanent added afterward misses that turn's upkeep entirely, and the test needed to wait for the seat's NEXT own precombat-main (turn 3) for its upkeep trigger to get a real chance to fire. With this card closed, **all 94 cards in the Nekusar, the Mindrazer decklist are now implemented** (CR 707.14, new "Prepared" mechanic) | `worker-05` | active | 2026-09-05 |
| `rules-prepared-enters-prepared` | Post-completion coverage sweep (beyond the Nekusar decklist): the "Prepared" mechanic's whole set turned out to have a huge number of "~ enters prepared." creatures — a static ETB state needing no trigger at all — that the Naktamun Lorespinner work hadn't covered, since that card only has the "becomes prepared via an upkeep trigger" shape. New `CardProfile.entersPrepared: boolean`, parsed and skip-listed the same way as other unconditional static flags, wired into `putOntoBattlefield` (`prepared: true` set at creation) and into the `preparedCast` gate (now computed for `entersPrepared` cards too, not only ones with a `become-prepared` trigger). One primitive, zero new engine concepts, **+16 cards flipped to fully implemented in a single pass** (verified via the project's own `data/rules/engine-card-profiles.json` export, cross-checked against every card whose sole remaining `unimplementedText` line was exactly "~ enters prepared."). Caught while writing the scenario test: the test's own `putOnBattlefield` helper builds a `Permanent` literal directly, bypassing `putOntoBattlefield`'s real entry logic entirely — had to cast the creature for real (hand + mana + `type: "cast"`) to actually exercise the new code path, and the resulting "cast a copy of Rampant Growth" then opened a real `search-library` `PendingChoice` that a naive `passUntil(stack.length === 0)` doesn't wait for (stack is already empty while the choice is pending) — resolved with an explicit `choose-library-card` action, same lesson as the Wizard Class draw-step timing catch earlier (CR 707.14) | `worker-05` | active | 2026-09-05 |
| `rules-prepared-trigger-templates` | Four more "Prepared" mechanic trigger templates, all reusing EXISTING trigger machinery with zero new engine concepts: "Whenever ~ attacks, it becomes prepared." (`attacks`/self, exercises the mechanic's newest event wiring for the first time — everything prior only tested `upkeep`/`first-main-phase`); "At the beginning of your upkeep, if ~ isn't prepared, it becomes prepared." (`upkeep`/you, unconditional — the "isn't prepared" guard is a no-op since setting an already-true flag to true again changes nothing, so it needed no new condition kind); "At the beginning of your first main phase, ~ becomes prepared." (`first-main-phase`/you, unconditional — reuses the event added for Black Market Connections); "Whenever you cast a creature spell, ~ becomes prepared." (`spell-cast`/you/`spellType: "creature"`, reusing the existing spell-type filter). Verified **+41** in the export count (9,460 → 9,501) — far more than the handful of cards directly targeted, since several `Emeritus of *` cards pair these templates with already-fully-supported iconic reprints (Lightning Bolt, Ancestral Recall, Demonic Tutor, Regrowth, Sign in Blood) whose back faces needed no new work at all | `worker-05` | active | 2026-09-05 |
| `c13-oracle-damage-triggered-shape` | Slice and Dice optional cycling trigger deals parameterized damage to each creature (CR 603.2, 603.5, 701.29) | `codex/c13-slice-and-dice-f99` | active | 2026-09-05 |
| `c13-oracle-create-token-triggered` | Sek'Kuar watches another nontoken creature you control die and creates a Graveborn token (CR 603.2, 603.6, 111.10) | `codex/c13-sekkuar-f99` | active | 2026-09-05 |
| `c13-oracle-other-activated-shape` | War Cadence applies a reusable variable generic-mana tax per creature blocking this turn (CR 602.2b, 509.1a, 117.8) | `codex/c13-war-cadence-a76` | active | 2026-09-05 |
| `c13-armillary-sphere-search` | Armillary Sphere's reusable two-basic-library search accepts the current Oracle reveal wording (CR 701.19, 701.19a) | `codex/c13-armillary-sphere-search-f99` | active | 2026-09-05 |
| `c13-spoils-of-victory-search` | Spoils of Victory's typed basic-land search is recognized across its split `Then shuffle` Oracle line (CR 701.19, 701.19a) | `codex/c13-spoils-of-victory-search-f99` | active | 2026-09-05 |
| `rules-prepared-landfall-combat-damage` | Two more Prepared trigger shapes: "Landfall — Whenever a land you control enters, ~ becomes prepared." (Tam, Observant Sequencer) reuses the existing `land-you-control` trigger subject already used by real Landfall abilities, plus the same `landfall\s+[—–-]\s*` ability-word-prefix stripping `matchTriggerLine` already applies elsewhere (caught only via a debug script — the front face literally starts with "Landfall — Whenever...", not "Whenever...", so the first version of this pattern silently failed to match at all); and "Whenever one or more creatures you control deal combat damage to a player, ~ becomes prepared." (Striding Shotcaller) reuses the existing `creature-you-control` subject against the `deals-combat-damage-to-player` event with zero new code — it fires once per qualifying creature each combat, which is harmless since `prepared` is already an idempotent flag. Scenario-tested end to end: playing a land triggers Tam; having a DIFFERENT creature (not Shotcaller itself) connect for combat damage still prepares Shotcaller, confirming the subject really is "any creature you control," not "self" | `worker-05` | active | 2026-09-05 |
| `rules-static-extra-land-drops` | New `CardProfile.extraLandDropsPerTurn` primitive recognizes the printed template "You may play {a\|an\|one\|two\|three} additional land(s) on each of your turns" (CR 305.2) and a new `maxLandDrops(state, seat)` engine helper sums the printed land drop, `player.extraLandDrops` (turn-limited grants), and every battlefield permanent's static `extraLandDropsPerTurn`, replacing the old inline `1 + player.extraLandDrops` expression at both the `legalActions` offering site and the `applyPlayLand` authoritative-check site so the two can never drift out of sync. Flips 6 cards fully implemented, including two format staples: Exploration and Azusa, Lost but Seeking (also Urban Evolution, Titania, The Explorer, Summer Bloom, Aesi Tyrant of Gyre Strait, Journey of Discovery among named oracle-text matches). Scenario-tested end to end: with Exploration in play a controller can play a 2nd land off a single land drop but is correctly blocked from a 3rd; with Azusa's own +2 stacked on the printed drop a controller can play 3 lands in one turn and is blocked from a 4th | `worker-05` | active | 2026-09-05 |
| `rules-aura-targeting-attachment` | Whole-mechanic gap closed: Auras previously had ZERO targeting or attachment support at all — grepping the export showed 1,228 distinct Aura oracle texts, with the top 5 "Enchant X" phrasings (creature 880, land 84, creature you control 57, player 43, permanent 24) covering 89% of them. Implemented the top 4 permanent-shaped ones (creature/land/permanent/creature-you-control — "Enchant player" Curses are left out of scope, see below): the "Enchant X" line now feeds directly into the existing `CardProfile.targetKind` (reusing all of `castableCard`'s targeting, `legalTargets`, and the CR 608.2b fizzle-on-illegal-target check for free, since an Aura's enchant restriction *is* its cast-time target restriction). On resolution the newly-created permanent is attached (`attachedTo`) to the chosen target (CR 303.4h). A new `CardProfile.auraModification` (reusing the existing `EquipmentModification` shape) recognizes "Enchanted creature gets ±N/±N[ and has KEYWORDS]" and "Enchanted creature has KEYWORDS", wired into `powerOf`/`toughnessOf`/`keywordOf` via new `attachedAuras`/`auraBonus` helpers mirroring the existing Equipment ones. Added the CR 704.5n state-based action: an Aura not attached to a legal object goes to its owner's graveyard, via a new `auraAttachmentLegal` predicate. Bug caught and fixed while wiring the SBA: the *existing* CR 704.5q Equipment-unattach loop iterated every permanent with `attachedTo` set with no subtype check at all — harmless while only Equipment ever set that field, but it would have ripped every land/permanent-enchanting Aura right back off on the very next state-based-action pass (since `isCreature(land) === false`); gated that loop to `hasSubtype(profile, "Equipment")` as part of this change. Verified **+213** in the export count (9,514 → 9,727) and set coverage 28.5% → 29.2%. Scenario-tested end to end: Hardened-Scale Armor attaches to a Bear and its +3/+3 shows up in `powerOf`/`toughnessOf`; Debilitating Injury's -2/-2 kills the enchanted Bear via ordinary lethal-damage SBA, and the Aura itself falls to the graveyard the same pass per 704.5n; Wild Growth (Enchant land) attaches to a Forest, proving the attach path isn't creature-only | `worker-05` | active | 2026-09-05 |
| `rules-untapped-enters-with-counters` | Found by request while triaging Commander 2014 for cards one line from complete: `parseEntersWithCounters` (the "~ enters with N counters on it" parser) already existed and was already wired into `putOntoBattlefield`, but the only skip-list line that consumed it out of `unimplementedText` required the word "tapped" between "enters" and "with" — so the untapped form (Pentavus, Spike Feeder, Kalonian Hydra, Swarm of Bloodflies, ...) sat permanently unimplemented even though the value was computed correctly and applied. Added the missing untapped skip line, anchored at "on it" so a trailing dynamic clause (Avatar of the Resolute: "...for each other creature you control with a +1/+1 counter on it") is deliberately left unconsumed rather than silently mis-parsed as a fixed amount. Also added a sibling template, `CardProfile.entersWithVariableCounters: {kind} \| null`, for the literal-"X" phrasing ("~ enters with X +1/+1 counters on it" — Walking Ballista, Hangarback Walker, Shivan Devastator, Mistcutter Hydra), wired at cast resolution to read the spell's own paid `{X}` (`object.variableValue`) into the same `additionalCounters` plumbing Auras/kicker already use. Verified **+41** in the export count (9,730 → 9,771) — 46 cards had this as their SOLE remaining blocker; a few of the 46 pulled in other still-unimplemented lines in this pass's accounting. Scenario-tested: Pentavus enters with exactly 5 counters (a fixed count with no "tapped"); Walking Ballista cast for X=2 enters with 2 counters and correctly shows 2/2 via `powerOf`/`toughnessOf`; cast for X=0 it enters 0/0 with no counters and immediately dies to ordinary lethal-toughness state-based actions, confirmed rather than assumed | `worker-05` | active | 2026-09-05 |
| `rules-tribal-lord-subtype-grants` | Another C14 one-liner: `StaticPowerToughnessGrant` already had an optional `subtype` field declared on the interface, but nothing in `staticPowerToughnessBonus` ever read it — dead scaffolding from an earlier pass. "Other Elves you control get +1/+1." (Imperious Perfect and 66 other tribal lords catalog-wide — Goblins, Cats, Spirits, Soldiers, Clerics, ...) was falling straight to `unimplementedText` with no recognizer at all. Added the recognizer (reusing the pre-existing `singularSubtype` helper — "Elves"→"Elf" — so no new pluralization logic was needed) producing a new `other-subtype-creatures-you-control` scope, and wired `grant.subtype` into the existing "you control" filter branch in `staticPowerToughnessBonus` alongside the pre-existing color filter. In the same pass, also added the "any controller" color anthem shape (Bad Moon: "Black creatures get +1/+1."; Celestial Crusader: "Other white creatures get +1/+1.") as `all-creatures`/`other-all-creatures` scopes, with the `powerOf`/`toughnessOf` global-bonus path gaining a color filter it never had (previously any "all-creatures" grant applied to every creature unconditionally, which happened to be harmless only because no colored "all-creatures" grant existed yet). Writing that filter surfaced a real, previously-uncaught bug in the pre-existing "Other [color] creatures you control get +N/+N" branch of the same function: it stored the color as `match[2].toUpperCase()` (e.g. "WHITE") instead of the single-letter form `cardProfile(...).colors` actually uses ("W"), via `.toUpperCase()` on the color WORD instead of the `DAMAGE_AMPLIFY_COLOR_LETTER` map already used elsewhere in the file for exactly this conversion — so that branch's color filter could never have matched anything, for any card, ever. Fixed both the new and pre-existing branches to go through `DAMAGE_AMPLIFY_COLOR_LETTER`. Verified **+34** combined in the export count (9,792 → 9,826) and set coverage 29.4% → 29.5%. Scenario-tested: Imperious Perfect boosts a same-seat Elf (1/1→2/2), leaves itself, a same-seat Bear, and an opponent's Elf unboosted; Bad Moon boosts a black creature under EITHER seat but leaves a green one alone; Celestial Crusader boosts a white creature under either seat but not itself | `worker-05` | active | 2026-09-05 |
| `rules-tribal-lord-explicit-creatures-phrasing` | A sibling tribal-lord phrasing found right after the above landed: "Other Goblin creatures you control get +1/+1." (Mad Auntie, Diregraf Captain, Merrow Reejerey, Dwynen Gilt-Leaf Daen, ...) spells out the literal word "creatures" between the subtype and "you control" — a different shape than "Other Elves you control get +1/+1." that the previous parser branch didn't cover, so it still fell into `unimplementedText`. Added a second regex branch producing the same `other-subtype-creatures-you-control` scope (the qualifier here is already singular in the Oracle text, so no `singularSubtype` call is needed, unlike the plural-noun phrasing). Some of these use a card TYPE instead of a creature subtype — Master of Etherium and Chief of the Foundry both read "Other artifact creatures you control get +1/+1." — so the engine-side check in `staticPowerToughnessBonus` now tests `hasSubtype(...) OR profile.types.includes(...)` against the same stored string rather than adding a second field. Verified **+13** in the export count (9,876 → 9,889). Scenario-tested with two synthetic fixtures: a Goblin lord boosts another Goblin but not itself or a Bear; an artifact-creature lord boosts another artifact creature (via the card-type path, not the subtype path) but not itself or a Bear | `worker-05` | active | 2026-09-05 |
| `rules-cedh-mana-vault-silence-flusterstorm` | Pivoted to cEDH staples by request. Three independent one-card primitives landed together: (1) new `TriggerCondition` kind `source-tapped`, the exact inverse of the pre-existing `source-untapped` (Howling Mine), closes Mana Vault's "At the beginning of your draw step, if ~ is tapped, it deals 1 damage to you." (2) New `SpellEffect` kind `opponents-cant-cast-spells-this-turn` plus a new `PlayerState.cantCastSpellsUntilEndOfTurn` flag (cleared every cleanup step, enforced once in `castableCard` so both `legalActions`'s offering path and `applyAction`'s authoritative cast path respect it for free) implements Silence. (3) New TargetKind `instant-or-sorcery-spell`, with `legalTargets` support and a widened `counter-target-spell(-unless-pay)` parser, distinguishes "Counter target instant or sorcery spell..." (Flusterstorm, Miscalculation, Daze) from the pre-existing generic "Counter target spell..." — the two are not interchangeable, since the narrower one must reject a legal creature-spell target that the generic one would accept. Verified via three separate exports: Mana Vault +1 (9,907→9,908), Silence +1 (9,908→9,909), the instant-or-sorcery-spell cluster +6 (9,909→9,915); set coverage 29.5%→29.9% across the batch. Scenario-tested: Mana Vault deals exactly 1 damage on the controller's draw step while tapped and none while untapped; Silence blocks the opponent's instant-speed cast for the rest of that turn only (confirmed cleared by their own next turn) while leaving the caster free to act; Flusterstorm's `legalTargets` accepts an on-stack Lightning Bolt but rejects an on-stack Grizzly Bears, then resolves the ordinary counter-unless-pay decline flow against the Bolt | `worker-05` | active | 2026-09-05 |
| `rules-cedh-city-of-traitors` | Cross-referenced `data/decks/cedh-pod.json` (a real 4-deck, 400-card imported cEDH pool already used by the "tested pod" Home mode) against the engine export to find named staples still unimplemented, rather than mining by pattern frequency. City of Traitors ("When you play another land, sacrifice this land.") needed a whole new trigger EVENT, not just a condition: the engine only raised events for object/player things happening (ETB, dies, life change, upkeep, ...), never for the ACTION of playing a land as distinct from that land's own "enters the battlefield." Added `TriggerEvent` kind `play-land` (raised once in `applyPlayLand`, right after `putOntoBattlefield`, alongside — not instead of — the land's own ETB event), a matching `TRIGGER_TEMPLATES` line, a `TriggerEvent`-keyed Spanish label, a client `abilities.ts` glyph entry (both `Record<TriggerEvent, ...>` maps are exhaustive and failed to compile until this was added), and a bare `Sacrifice ~` sentence recognizer (only the "Sacrifice ~. If you do, ..." compound form existed before). Verified **+3** in the export count (9,919 → 9,922 — City of Traitors has three printings, each its own profile) and set coverage holds at 29.9%. Scenario-tested: with City of Traitors already on the battlefield, playing a Forest from hand fires the trigger, which resolves and sends City of Traitors to its owner's graveyard while the Forest stays in play | `worker-05` | active | 2026-09-05 |
| `rules-cedh-sacrifice-creature-cost` | Diabolic Intent and Culling the Weak (both in `cedh-pod.json`) share "As an additional cost to cast ~, sacrifice a creature." — the mana-cost sibling `additionalCostSacrificeLand` already existed (Harrow), so this is the exact same shape with `isCreature` in place of `isLand`. Added `CardProfile.additionalCostSacrificeCreature`, wired into `applyCast` identically to the land case (auto-picks the first qualifying creature — the same simplification the pre-existing land case already makes, not a new corner cut), plus one thing the land case was missing: a `castableCard` legality gate so a player with no creature to sacrifice never sees the spell offered as castable at all, rather than getting a thrown error only at execution time. Verified **+21** in the export count (9,927 → 9,948) and set coverage 29.9% → 30.0%. Scenario-tested: casting Diabolic Intent with a Bear on the battlefield sacrifices the Bear and opens the ordinary `search-library` tutor choice, landing the chosen card in hand; with zero creatures on the battlefield the cast is absent from `legalActions` and throws if forced | `worker-05` | active | 2026-09-05 |
| `rules-persist-and-zulaport-drain` | Second target deck this session: cross-referenced a user-supplied Prossh, Skyraider of Kher decklist (97 unique cards) the same way as the cEDH pod, surfacing two more one-line gaps. (1) The bare "Persist" reminder line (and its "Undying" sibling) was never skip-listed in `recognizeText`, even though both keywords already synthesize a real `undying-return` trigger from `card.keywords` — the exact same class of gap as the untapped enters-with-counters fix earlier this session (value computed correctly, consuming line missing). Added both skip lines, mirroring the existing `rebound`/`extort` pattern. (2) Zulaport Cutthroat's "Whenever ~ or another creature you control dies, each opponent loses 1 life and you gain 1 life." is a DIFFERENT template from Blood Artist's "...target player loses 1 life and you gain 1 life." (single chosen target vs. every opponent at once) — the underlying `each-opponent-loses-life` effect already existed and had a bare-sentence recognizer, so this only needed the compound "...and you gain N life" tail, mirroring the existing "Draw N cards and each opponent loses N life" compound pattern. Verified **+50** combined in the export count (9,948 → 9,998) — the Zulaport template alone matched 126 catalog-wide unique oracle texts, by far the largest single-regex win of the whole session. Set coverage 30.0% → 30.2%. Scenario-tested: a synthetic Persist creature returns once with a -1/-1 counter after dying, then stays dead on a second death; Zulaport Cutthroat drains 1 from every opponent and gains its controller 1 life when a DIFFERENT creature the controller owns dies | `worker-05` | active | 2026-09-05 |
| `rules-skullclamp-equipped-creature-dies` | The `equipped-creature` `TriggerSubject` already existed end-to-end in the engine — `matchesSubject` already handled it ("the watcher is the Equipment; the event object must be the creature it is attached to", explicitly commented "(Skullclamp, Argentum Armor)") — but no `TRIGGER_TEMPLATES` entry ever produced a trigger with that subject, so the machinery sat unused. Added the two entries: "Whenever equipped creature dies, ..." and "Whenever equipped creature attacks, ..." (both zero new engine code, pure data-table additions). The export count crossed the 10,000 fully-implemented milestone in this same cycle (9,998 → 10,050), though that delta also folds in upstream contributions merged in alongside this change; Skullclamp and Argentum Armor are both confirmed `fullyImplemented: true` by name. Set coverage 30.2% → 30.3%. Scenario-tested: equipping Skullclamp onto a Grizzly Bears (2/2 → 3/1 from the clamp's own +1/-1) leaves it alive; a burn spell then kills the now-1-toughness Bear and the death trigger draws its controller two cards, net +1 hand size after accounting for the burn spell itself leaving hand to be cast | `worker-05` | active | 2026-09-05 |
| `rules-natural-order-green-restriction` | Natural Order (Prossh decklist) is a two-line color-restricted sibling of the sacrifice-a-creature primitives added earlier this session: "sacrifice a green creature" as the additional cost, and "search your library for a green creature card" as the effect. New `CardProfile.additionalCostSacrificeCreatureColor: string \| null`, wired identically to `additionalCostSacrificeCreature` (both a `castableCard` legality gate and the `applyCast` sacrifice, filtered by color this time). The tutor half exposed a real, previously-unnoticed correctness gap: the shared `searchCriterion` parser (used by every "Search your library for a [criterion] card" template) had no concept of color at all — a leading color adjective like "green" fell through every check and was silently dropped, meaning a color-restricted search would have quietly become an unrestricted one the moment a card using this shared helper needed it. Added color detection to `searchCriterion` (stripped before type/subtype parsing, since "green" is not a valid subtype), a new optional `colors` field on the `search-library` effect, and a matching filter in the engine's own search-resolution code. Verified **+1** (10,050 → 10,051; Natural Order specifically). Scenario-tested: with only a red creature to sacrifice, Natural Order isn't offered as castable and throws if forced; with a green creature available, casting it sacrifices the green creature (leaving an unrelated red one untouched), then the tutor choice accepts a green creature from the library but rejects a red one (confirmed via a thrown error, not just an absent option) | `worker-05` | active | 2026-09-05 |
| `rules-beast-within-permanent-token` | Beast Within/Generous Gift ("Destroy target permanent. Its controller creates a 3/3 green Beast/Elephant creature token.") had an exact sibling already implemented: `destroy-target-creature-then-controller-token` (An Offer You Can't Refuse-style cards restricted to "Destroy target CREATURE..."). Its engine handler is target-agnostic — it destroys whatever `object.targets[0]` resolves to and hands the token to THAT permanent's controller, regardless of what kind of permanent it was — so this needed zero new engine.ts code, only a sibling parser branch in `recognizeText`'s closed-template section matching "Destroy target permanent...` instead of "...creature...", reusing the exact same effect kind with `targetKind: "permanent"` in place of `"creature"`. Verified **+2** in the export count (10,051 → 10,053) and set coverage 30.3% → 30.4%. Scenario-tested: casting Beast Within on an opponent's Grizzly Bears destroys it and creates the 3/3 Beast token under the OPPONENT's control, not the caster's — the whole point of the card, and the part a naive "give myself a token" implementation would get backwards | `worker-05` | active | 2026-09-05 |
| `rules-x-mana-value-tutor` | Green Sun's Zenith ("Search your library for a green creature card with mana value X or less, put it onto the battlefield, then shuffle.") needed a genuinely new `search-library` restriction: none of the existing regexes handled a trailing "with mana value X or less" clause, since the shared `single` regex requires a literal comma immediately after the word "card" — this text has "card with mana value X or less," instead. Added a dedicated branch, a new optional `maxManaValue?: "X"` field on the `search-library` effect, and a matching filter in the engine's resolution code reading the spell's own paid `{X}` off the stack object (`object.variableValue`) — the same value already used for the untapped-`X`-counters and Silence-adjacent primitives earlier this session. Verified **+2** in the export count (10,053 → 10,055): Green Sun's Zenith and Wargate (a colorless, any-permanent sibling of the same template). Chord of Calling shares the same search line but still needs Convoke — an entirely separate, unimplemented alternate-cost mechanic — noted as a boundary rather than pulled into scope here. Scenario-tested: casting Green Sun's Zenith for X=2 opens a tutor choice that rejects a green creature with mana value 6 (over budget) but accepts one with mana value 1, landing it on the battlefield | `worker-05` | active | 2026-09-05 |
| `rules-static-mana-ability-grants` | Whole-mechanic gap closed: "X [you control] have '{T}: Add ...'" (Chromatic Lantern, Joraga Treespeaker, Cryptolith Rite, Manaweft Sliver, 44 catalog-wide) had no support at all — `manaAbilities` was read straight off the permanent's own `CardProfile` at every consumption site, with no notion of a mana ability GRANTED by another permanent's static ability. Added `CardProfile.staticManaAbilityGrants` (scope `you-control`/`all`, optional `excludesSelf`/`type`/`subtype`/`minLevel`, reusing the existing `ManaAbility` shape and the existing `parseManaAbilities` line-parser on the quoted inner text so no new mana-ability grammar was needed) and a new `grantedManaAbilities`/`manaAbilitiesFor` pair in the engine that re-indexes granted abilities after a permanent's own printed ones so they never collide; wired into all three real consumption sites (`manaSources` for automatic payment planning, `legalActions`'s offering loop, and `applyActivateMana`'s authoritative lookup — previously each read `profile.manaAbilities` or `cardProfile(...).manaAbilities` directly). Joraga Treespeaker's grant only applies at LEVEL 5+, not from level 0; the parser tracks the nearest preceding "LEVEL N[+/-M]" marker line the same way `parseLevelDefinitions` already does for power/toughness/keywords, attaching a `minLevel` the engine checks against `permanent.counters.level`. Verified **+11** combined in the export count (10,055 → 10,066, across two passes — the second closing the bare-subtype phrasing gap Joraga Treespeaker itself uses, "Elves you control have..." with no "creatures" noun) and set coverage 30.4% → 30.5%. Scenario-tested: Chromatic Lantern lets its controller tap a Forest for blue mana, not just green; an Elf gains no mana ability while Joraga Treespeaker sits below level 5, then gains "{T}: Add {G}{G}." the instant a level counter pushes it to 5 | `worker-05` | active | 2026-09-05 |
| `rules-stampede-family` | Overwhelming Stampede's exact effect ("creatures you control gain trample and get +X/+X until end of turn, where X is the greatest power among creatures you control") already existed as a dedicated `overwhelming-stampede` kind, but only matched with "Until end of turn," as a LEADING clause. Pathbreaker Ibex's attack trigger has the identical effect with "until end of turn" TRAILING instead, so the existing regex missed it entirely; added a sibling regex reusing the same effect kind and handler verbatim (zero new engine code). Craterhoof Behemoth is the same template with a different X formula (creature count, not greatest power), so it got its own new `creature-count-stampede` kind sharing the same handler body (merged into the same `case` label, branching only on which `effect.kind` matched). Verified **+2** in the export count (10,066 → 10,068) and set coverage holds at 30.5%. Scenario-tested: casting Craterhoof Behemoth with two Grizzly Bears already in play pumps all three creatures (Craterhoof counts itself) by +3/+3 with trample; attacking with Pathbreaker Ibex (3 power) alongside a Grizzly Bears (2 power) pumps both creatures by +3/+3 with trample, using Ibex's own power as the greatest among them | `worker-05` | active | 2026-09-05 |
| `rules-oracle-of-mul-daya-top-of-library` | Oracle of Mul Daya (Prossh decklist) needed two new static primitives: `CardProfile.playLandsFromTopOfLibrary` ("You may play lands from the top of your library") and `CardProfile.revealsTopOfLibrary` ("Play with the top card of your library revealed") — its third line, "You may play an additional land on each of your turns," was already covered by the pre-existing `extraLandDropsPerTurn` (Exploration/Azusa). Wired the first into `legalActions` (a `play-land` action is now also offered for the controller's own top library card when a permanent grants that permission) and `applyPlayLand` (accepts a `cardId` from either the hand or that exact top card, shifting the library instead of filtering the hand). The reveal line is genuine public information (CR 108.4), not merely visible to its own controller — added the one deliberate, narrowly-scoped exception to `projection.ts`'s "opponent's library is structurally absent" boundary: a new `PlayerView.revealedTopLibraryCard` exposed to EVERY viewer (including opponents) whenever that player controls a revealing permanent, gated on an explicit static ability rather than a general leak. Verified **+4** in the export count (10,068 → 10,072; Oracle of Mul Daya's four printings) and set coverage holds at 30.5%. Scenario-tested: the top-of-library land drop is offered and correctly shifts the library (not the hand) on resolution; it is NOT offered for a player without the static; the top card appears in both the controller's own projection and the opponent's; a player without the reveal static keeps `revealedTopLibraryCard` undefined even for opponents | `worker-05` | active | 2026-09-05 |
| `rules-ogre-battledriver-event-creature-keyword` | Ogre Battledriver (Prossh decklist) needed a new `modify-event-creature-and-grant-keyword` effect kind: the closest existing template, `modify-triggered-creature-and-grant-keyword`, targets the trigger's OWN source (`sourcePermanentId`), while this card's "that creature gets +2/+0 and gains haste" refers to the OTHER creature named by the entering-creature event (`eventPermanentId`) — the same self-vs-event distinction the codebase already drew for the plain (no-keyword) pump kinds. Parsing it surfaced a genuine, previously-unnoticed matching bug: the shared `TRIGGER_TEMPLATES` entry for "Whenever [a/another] creature you control enters" used an optional `(?:a|another)?` group but always assigned the self-inclusive `creature-you-control` subject no matter which word matched, so every card phrased with "another creature you control enters" was silently missing CR 109.5's "another excludes the source" rule. Split it into two ordered templates (the "another" one checked first) rather than touching the shared regex in place, so the bare "a creature you control enters" phrasing keeps its existing self-inclusive behavior untouched. Verified **+1** in the export count (10,072 → 10,073) and set coverage holds at 30.5%; the full rules suite grew to **731** tests and stayed green through the split, confirming no other card's subject regressed. Scenario-tested: casting a Grizzly Bears while Ogre Battledriver is already on the battlefield pumps only the ENTERING bear to 4/2 with haste, leaving Battledriver's own power/toughness/keywords untouched | `worker-05` | active | 2026-09-05 |
| `rules-reflecting-pool-vexing-shusher` | Two independent Prossh decklist gaps closed together. Reflecting Pool ("{T}: Add one mana of any type that a land you control could produce") is functionally identical to the existing Fellwar Stone/Harvester Druid "any COLOR that a land could produce" board-dependent mana template — `ManaAbility.anyColorFromLandsControlledBy`/`colorsFromLandsControlledBy` already union every `ManaType` including `C`, so "type" vs "color" was a wording gap, not a semantic one; widened both the construction regex and its coverage check to accept either word rather than adding a parallel path. Vexing Shusher ("{R/G}: Target spell can't be countered") needed a new `make-target-spell-uncounterable` effect kind: `StackObject.cantBeCountered` and `canCounterSpell`'s check of it already existed for cast-time uncounterable spells (Delighted Halfling); the only gap was an ability that sets that flag on an EXISTING stack entry, targeted exactly like `counter-target-spell` already targets one (`target.kind === "spell"` / `target.stackId`). Added to the SHARED `recognizeSentence` grammar (not a card-specific branch), so it flows through the same generic activated-ability fallback every other targeted activation uses. Verified **+2** combined in the export count (10,073 → 10,076) and set coverage holds at 30.5%. Scenario-tested: Reflecting Pool with a Mountain and a Forest in play offers exactly `{R, G}`; casting a Lightning Bolt then activating Vexing Shusher targeting it on the stack flips `cantBeCountered` on that exact entry and `canCounterSpell` from true to false | `worker-05` | active | 2026-09-05 |
| `rules-garruk-wildspeaker-untap-two-lands` | Garruk Wildspeaker (Prossh decklist) was missing only its "+1: Untap two target lands" line; its −1 and −4 abilities already worked. Generalized the existing single-target `untap-target-permanent` effect (previously hardcoded to `object.targets[0]`) to fold over every `"permanent"` entry in `object.targets`, a no-op change for every existing single-target user. Parsing "two target lands" surfaced a real bug: `recognizeSentence` already supports returning `targetKinds` for multi-target abilities, but `parseActivatedAbility`'s LOYALTY-ability branch (planeswalker `+N`/`−N` costs) built its own return object and never forwarded `recognized.targetKinds` — so ANY planeswalker ability needing more than one target of the same kind would have silently lost every target past the first. Fixed by forwarding it the same way the ordinary mana-cost-ability path already did. Also closed a real gap in the shared `targetKinds` machinery itself: it had no distinctness check (CR 601.2c forbids choosing the same object twice for one instance of "target"), so a "two target lands" ability with only one land in play would previously have offered itself as activatable and then silently accepted the same land for both slots. Added a legality-time check requiring enough DISTINCT candidates when repeated slots share a kind, and an execution-time rejection of duplicate chosen targets. Verified **+11** in the export count (10,076 → 10,087: Garruk's own printings plus every other planeswalker the loyalty-targetKinds fix unblocked) and set coverage 30.5% → 30.6%. Scenario-tested: activating +1 targeting two named lands untaps exactly those two and leaves a third tapped land alone, plus gains a loyalty counter; targeting the same land twice throws; the ability isn't offered at all with only one land in play (documented boundary: the exact-count model doesn't yet support "up to N, minimum 1") | `worker-05` | active | 2026-09-05 |
| `rules-look-top-reorder` | Whole-mechanic gap closed: "Look at the top N cards of your library, then put them back in any order" (Ponder's first line, Sensei's Divining Top, Sage Owl, Halimar Depths, Mirri's Guile — 21 catalog cards) had zero support, since unlike Scry/Surveil no card ever leaves the top group, only the sequence changes; neither existing choice shape fit. Added a new `look-top-reorder` `SpellEffect`, a new `reorder-top` `PendingChoice` (private to the choosing seat via a new `ReorderTopView` in `projection.ts`, mirroring `ScryView`), and a new `reorder-top` `GameAction` that submits the revealed cards' full permutation in one action (validated as a true permutation — same set, same length, no repeats) rather than Scry's per-card sequential decisions, since reordering has no natural "send this one away" branch point. `legalActions` offers one representative action (keep the current order — itself a fully legal choice) so bots always have something to take. Implemented as a single `case` in the SHARED `applyEffect` switch, so it works identically through a spell, an activated ability, or a triggered ability with no per-path special-casing — this is what let Sage Owl and Halimar Depths (triggered) start working for free alongside Sensei's Top (activated). Sensei's Divining Top additionally needed its own `draw-then-source-to-library-top` effect kind for "{T}: Draw a card, then put this artifact on top of its owner's library," using the `StackObject.sourcePermanentId` field activated abilities already carry to find and relocate itself. Verified **+13** in the export count (10,087 → 10,100) and set coverage holds at 30.6%. Ponder itself stays unimplemented — its trailing "You may shuffle" is a distinct, deliberately deferred clause. Scenario-tested: activating the {1} ability opens a private reorder choice over exactly the top three cards; submitting an explicit permutation reorders the library to match; a list that repeats or omits a card throws; the {T} ability draws the named card and leaves the Top itself as the new top card, removed from the battlefield | `worker-05` | active | 2026-09-05 |
| `rules-atarka-tribal-attack-trigger` | Atarka, World Render (Prossh decklist) needed a genuinely new subject-filter primitive: every existing `TriggerSubject` is a fixed string with no way to say "the triggering object must ALSO have subtype X" the way board-count `condition`s work — this needed a filter on the EVENT OBJECT itself. Added `TriggerDefinition.requireSubtype` as a sibling to the existing `excludeSubtype` field (Requiem Angel's "another non-Subtype creature... dies" already proved the field-alongside-subject pattern), checked once in the shared subject-matching function. Parsed via its own dedicated regex block outside the flat `TRIGGER_TEMPLATES` array (mirroring how Requiem Angel's line is built), since the subtype word must flow into the new field, not just the effect text. "it gains double strike until end of turn" needed one more fix: the general trigger-building path normalizes a leading "it" to "~" (self), which would be wrong here since "it" means the ATTACKING creature, not Atarka — this new block passes the effect text to `recognizeSentence` unnormalized, and a new "it gains KEYWORD until end of turn" pattern there reuses the `modify-event-creature-and-grant-keyword` kind (added for Ogre Battledriver earlier this session) with power/toughness both 0 — a pure keyword grant. Verified **+10** in the export count (10,100 → 10,110) and set coverage 30.6% → 30.7%. Scenario-tested: attacking with Atarka, a second Dragon, and a Grizzly Bears together grants double strike to both Dragons but not the Bear, with power/toughness unchanged on all three | `worker-05` | active | 2026-09-05 |
| `rules-beastmaster-ascension-counter-anthem` | Beastmaster Ascension's trigger line was already covered by the existing generic `add-counter-source` template; only its static anthem ("As long as ~ has seven or more quest counters on it, creatures you control get +5/+5") was missing. Found that `StaticPowerToughnessGrant` already declared `counterName`/`threshold` fields that were never read anywhere in `engine.ts` — added ahead of a card that needed them and never wired up. Added the missing half: a new `creatures-you-control-source-counter-threshold` scope, a parser branch with word-number threshold parsing via the shared `toNumber` (matching every other counted-quantity pattern), and a second pass in `staticPowerToughnessBonus` checking the GRANTING permanent's own counters before adding its bonus to every creature the same controller owns — separate from the existing wide "creatures-you-control" filter, which has no way to gate on the source's own state. Verified **+1** in the export count (10,110 → 10,111) and set coverage holds at 30.7%. Scenario-tested: at six quest counters every controlled Grizzly Bears stays 2/2; at seven counters both Bears pump to 7/7 | `worker-05` | active | 2026-09-05 |
| `rules-forbidden-orchard-taps-for-mana` | Forbidden Orchard needed a genuinely new trigger event: the existing `becomes-tapped` fires for ANY tap (Icy Manipulator-style effects included) and would over-trigger, since this card must fire specifically when tapped to activate a mana ability. Added `TriggerEvent`/`GameEvent` variant `taps-for-mana` (carrying `permanentId`/`controller`/`card` like every other object event, so the existing generic `eventObject` helper needed no changes), raised from `applyActivateMana` alongside the existing `raiseTapEvents` call, gated on `ability.requiresTap`. As with `play-land` earlier this session, TypeScript's exhaustiveness checking on `Record<TriggerEvent, ...>` maps caught two real spots needing updates before either `npm run check` workspace would pass: a `default:` branch in `engine.ts`'s event-to-log-text switch, and the client's `TRIGGER_GLYPHS` map in `abilities.ts`. Parsing this card also surfaced a real, unrelated bug affecting ~97 catalog cards: the shared token-descriptor parser (`parseCreateToken`) strips exactly five color words but never recognized "colorless," so "a 1/1 colorless Spirit creature token" produced the correct empty `colors` array but a wrong `typeLine` of "Creature — colorless Spirit" instead of "Creature — Spirit." Fixed by adding "colorless" to the same grammar-word exclusion list as "artifact"/"creature"/"and". Verified **+2** in the export count (10,111 → 10,113) and set coverage holds at 30.7%. Scenario-tested: tapping Forbidden Orchard for mana gives the OPPONENT a 1/1 Spirit with the corrected type line and empty colors; a plain non-mana tap raises no token | `worker-05` | active | 2026-09-05 |
| `rules-lotus-cobra-any-color-mana` | Lotus Cobra ("Landfall — ... add one mana of any color") needed a new `add-mana-any-color` effect kind — a one-shot resolution requiring a runtime color choice, unlike a mana ABILITY (the deterministic-ritual "Add {W}{U}{B}{R}{G}" pattern deliberately never matches "any color", per its own existing comment). Reused the `choose-color` `PendingChoice` built for `return-all-permanents-of-color`/`damage-all-creatures-of-color` rather than a parallel mechanism, widening its `effect` type union. Those two existing consumers distinguish "still needs a choice" from "color already chosen" via a `color: "chosen"` discriminant checked against `object.chosenColor`; `add-mana-any-color` has no fixed-color variant to discriminate against, so it checks `object.chosenColor` presence directly instead. Reusing `choose-color` surfaced a real bug the two existing consumers never triggered: `applyChooseColor` unconditionally moved the choice's source card to the graveyard after resolving — valid only when the source is a resolving SPELL, but Lotus Cobra is already ON the battlefield when its trigger fires, so this would have incorrectly binned it the moment a color was chosen. Added a `sendSourceToGraveyard: boolean` field to the choice (`true` for the two existing consumers, `false` here) gating that move. Verified **+3** in the export count (10,113 → 10,116) and set coverage holds at 30.7%. Scenario-tested: playing a land with Lotus Cobra in play opens a color choice; choosing blue adds `{U}` and leaves Lotus Cobra on the battlefield, confirmed absent from the graveyard | `worker-05` | active | 2026-09-05 |
| `rules-mana-crypt-coin-flip` | Mana Crypt's mana ability was already covered generically; its upkeep trigger ("flip a coin, if you lose, deal 3 damage to you") needed a real randomness primitive the engine had never modeled as a card effect. Reused the engine's existing seeded `GameState.rngState`/`nextRandom` (today used only for shuffling and a couple "destroy a random permanent" effects) rather than adding a second randomness source — required, since the whole simulator and test suite depend on games being exactly reproducible from their seed. Added `coin-flip-self-damage-if-lost` and an `applyEffect` case that rolls, advances `rngState`, and on a loss reuses the existing `dealDamageFromObject` helper (already builds the right `DamageSource` from a triggered ability's `StackObject`) — no new damage logic needed. Scenario-testing surfaced a real trap in the TEST HARNESS, not the engine: `passUntil` can never catch "the trigger is visible on the stack" for a mandatory, choiceless trigger, because the engine resolves it atomically between two priority-decision points with nothing external to act on in between — an early attempt spun through 60+ turns of REAL coin flips before hitting the step budget. Fixed by asserting on the effect (first observed life change, or a turn count reached with life unchanged) instead of an intermediate stack state that's never actually observable at this granularity. Verified **+1** in the export count (10,116 → 10,117) and set coverage holds at 30.7%. Scenario-tested with two `rngState` seeds precomputed to land on either side of the 0.5 threshold: one deterministically loses the first flip for exactly 3 damage, the other deterministically wins and stays at full life | `worker-05` | active | 2026-09-05 |
| `rules-fix-partial-target-legality-overreach` | Fixed a real regression from upstream commit `25d298e` ("Resolve spells with partially illegal targets"), caught while merging: a blanket legality gate added at the top of `applyEffect` (`if (targetIndex >= 0 && object.targets.length && !targetIsLegal(target)) return state;`) checked EVERY recursive call against `object.targets[targetIndex]`, even for a sub-effect with no target concept — a `compound`'s children default to the SAME inherited `targetIndex` unless `targetOffsets` says otherwise, so "Destroy target creature. Draw a card." would skip the untouched draw the moment the destroy target became illegal. Broke three pre-existing regression tests (nonblack-destroy-then-draw, a C13 multi-target damage scenario, a kicker "then draw" bonus). The commit's own new test still passes without the gate, since the effect it actually covers (`damage-divided-targets`) already loops over `object.targets` internally and never depended on the blanket check. Fixed by removing just those three lines, keeping the other, correctly-scoped half of the same commit (the `resolveTop`-level change from "any illegal target fizzles the whole spell" to "fizzle only when ALL targets are illegal," CR 608.2b) intact. Verified: all three previously-broken tests pass again, the commit's own new Fissure Vent test still passes, full **780**-test suite green | `worker-05` | active | 2026-09-05 |
| `rules-pattern-of-rebirth-enchanted-creature-search` | Two genuinely new, reusable primitives. First, a `TriggerSubject` for Auras — `"enchanted-creature"` — a direct parallel to the existing `"equipped-creature"` subject built earlier this session for Skullclamp, same shape and timing guarantee (SBAs haven't removed the Aura yet when its host's "dies" event fires). Second, and more consequential: `search-library` as a TRIGGERED ability's effect had never actually worked, despite being one of the most reused effect kinds in the engine — the interactive choice that opens it lives entirely in `resolveTop`, special-cased only for a resolving SPELL or an ACTIVATED ability; the shared `applyEffect` switch has `case "search-library": return state;`, a deliberate no-op pointing at that pre-check. The `if (object.trigger)` branch already special-cases `scry`/`surveil`/`look-top-select`/`drawUpTo` the same way but never `search-library`, so a card whose own trigger needs to search silently did nothing. Added a matching `triggerSearch` branch scoped to what a triggered search actually needs (type/subtype/color filtering, empty-library shuffle), without the spell/activated path's "up to N" auto-fetch or `maxManaValue` handling that no triggered search in the catalog needs today. Also fixed a real gap in the pre-existing `eventControllerChoice` machinery: its captured clause is naturally third-person ("search THEIR library"), but every shared effect template hard-codes first person, so the clause never matched anything — normalized with a targeted `.replace(/\btheir\b/gi, "your")` on just that captured text. Verified **+9** in the export count (10,197 → 10,206) and set coverage holds at 31.2%. Scenario-tested: enchanting a Grizzly Bears with Pattern of Rebirth, then killing it with a Lightning Bolt, opens a library search offering only creature cards (excluding a Sol Ring in the same library); choosing the creature card puts it directly onto the battlefield | `worker-05` | active | 2026-09-06 |
| `rules-survival-of-the-fittest-creature-discard` | Survival of the Fittest ("{G}, Discard a creature card: Search your library for a creature card...") needed a new `discardsCreatureCard` cost flag, a type-restricted sibling of the existing `discardsCard`, threaded through the same four sites `discardsCard` already touches (`legalActions`' action enumeration, `activatableAbility`'s legality gate, and both of `applyActivate`'s discard-resolution blocks) so a non-creature card can never satisfy this cost. The real find: `parseActivatedAbility`'s final "leftovers" validation already stripped "discard a card"/"discard one card" but had no pattern for "discard A CREATURE card", so the whole ability was silently rejected even though its cost fields and its `search-library` effect were both already parsing correctly in isolation — they just never reached the same return statement. Fixed with one more `.replace()` in that leftovers chain. Verified **+7** in the export count (10,206 → 10,213) and set coverage holds at 31.2%. Scenario-tested: activating with a chosen Grizzly Bears as the discard puts it in the graveyard and opens a search offering only creature cards; the same ability is correctly NOT offered when the specified discard target is a non-creature card already in hand | `worker-05` | active | 2026-09-06 |
| `rules-beseech-the-queen-lands-cap-search` | Beseech the Queen widens the existing `search-library` `maxManaValue` field from a bare `"X"` discriminant (Green Sun's Zenith's paid `{X}`) to `"X" | "lands-you-control"` — a board-dependent cap read live at resolution rather than a value paid at cast time, computed the same way every other "number of lands you control" scaling already reads the board. Found via `tools/rules/identify_near_complete_cards.py`, which correctly flagged this as `priority: "reuse-existing"` against the `search-library` family rather than a new primitive. Verified **+1** in the export count (10,213 → 10,214) and set coverage holds at 31.2%. Scenario-tested: with three lands in play, a search offers a 2-mana-value creature and a 1-mana-value artifact but excludes a 4-mana-value creature in the same library | `worker-05` | active | 2026-09-06 |
| `rules-protean-hulk-any-number-total-mv` | Protean Hulk needed an open-ended sibling of the existing `search-library-multi` family: every prior template there picks a FIXED count (`destinations` sized to the exact number of slots, auto-finishing once `selectedIds.length` reaches `destinations.length`). This card's pick count is unbounded, capped only by a running mana-value BUDGET, so it needed a new `maxTotalManaValue?: number` field and a different stopping rule — the player accepts or rejects each pick against the remaining budget and explicitly calls the already-generic `finish-library-search` action when satisfied (no new action type needed, only a new field-driven branch in `applyChooseMultiLibraryCard`/`finishMultiLibrarySearch` that puts every selected card onto the battlefield untapped instead of consulting per-slot destinations). Also extends the Pattern-of-Rebirth-era triggered-search wiring in `resolveTop` with a sibling `triggerSearchMulti` branch, since this is also a dies-triggered search. Found via `identify_near_complete_cards.py`'s "reuse-existing" flag against the same family. Verified **+1** in the export count (10,214 → 10,215) and set coverage 31.2% → 31.3%. Scenario-tested: killing Protean Hulk with two 2-mana-value creatures and one 4-mana-value creature in the library lets the controller pick both 2-drops (running total 4), throws when a third pick would exceed 6, and battlefields exactly the two picked creatures untapped once the search is explicitly finished | `worker-05` | active | 2026-09-06 |
| `rules-somberwald-sage-creature-spell-mana-restriction` | Generalized the `ManaRestriction` primitive (previously Delighted-Halfling-only, `kind: "legendary-spell"`) to a second kind, `"creature-spell"`, unlocking Somberwald Sage ("{T}: Add three mana of any one color. Spend this mana only to cast creature spells."). Widened `ManaRestrictionKind` in `mana.ts`, then renamed the boolean `allowLegendaryMana` plumbing across ~20 call sites in `engine.ts` (`manaSources`, `planManaPayment`, `shouldPromptManaPayment`, `manualManaPlan`, `beginManaPayment`, the `mana-payment` `PendingChoice` field, `payPlayerCost`, and both cast-computation sites) to a generalized `allowedRestrictions?: readonly ManaRestrictionKind[]`, computed once per cast via a new `allowedManaRestrictions(profile)` helper (Legendary supertype → `"legendary-spell"`, `isCreature(profile)` → `"creature-spell"`). In `characteristics.ts`, added a second `manaRestriction` parser branch for "Spend this mana only to cast creature spells." and — separately — widened the `restrictedManaLine` coverage-recognition regex inside `recognizeText` (the gate a card's `fullyImplemented` flag depends on) to the same wording; missing that second site would have left the card correctly playable but incorrectly flagged as not-fully-implemented. Confirmed via probe that Delighted Halfling's existing behavior (including `makesSpellUncounterable`) is unchanged. Verified **+1** in the export count (10,215 → 10,216); set coverage holds at 31.3%. Scenario-tested: activating for {R} tags three red mana restricted to creature spells; an instant in hand cannot be cast with it (absent from `legalActions`), while a same-cost creature spell can and resolves onto the stack. Food Chain, the other `identify_near_complete_cards.py`-flagged card sharing this restriction, needs a genuinely separate primitive (an exile-a-creature mana-ability cost distinct from the existing sacrifice-only cost model, plus a dynamic "1 + exiled creature's own mana value" amount) and is deferred | `worker-05` | active | 2026-09-06 |
| `rules-food-chain-exile-creature-manavalue-mana` | Food Chain ("Exile a creature you control: Add X mana of any one color, where X is 1 plus the exiled creature's mana value. Spend this mana only to cast creature spells.") needed the second, genuinely separate primitive deferred from the Somberwald Sage pass above. Added `ManaAbility.exilesCreature` (activation cost: exile one unrestricted creature you control) and `ManaAbility.amountFromExiledManaValuePlusOne` (amount read from the exiled permanent's own `manaValue` at activation time), mirroring the shape of the pre-existing `sacrificesCreatures`/`amountFromSacrifice` pair (Springjack Pasture) as closely as the mechanic allows. New `exileCreatureCandidates` helper in `engine.ts` (parallel to `manaSacrificeCandidates`, no subtype filter, exile zone instead of graveyard), a `canUseManaAbility` gate requiring a legal candidate, a `legalActions` branch enumerating one action per candidate-creature-x-color-choice (no outer amount loop since the amount is not a player choice), a new `exileId?: string` field on the `activate-mana` action, and `applyActivateMana` reading the chosen creature's mana value before exiling it and threading it through the existing `amount`/`restrictedOutput` computation. Also excluded `exilesCreature` abilities from the automatic-tap-source list in `manaSources`, same reasoning as `sacrificesCreatures`. Needed the same two-site parser fix as Somberwald Sage (`characteristics.ts`'s `parseManaAbilities` builder AND the separate `recognizeText` coverage gate). Verified **+1** in the export count (10,216 → 10,217); set coverage holds at 31.3%. Scenario-tested: exiling a 2-mana-value Grizzly Bears adds exactly 3 mana of the chosen color restricted to creature spells, moves the Bears to exile (not graveyard), blocks an instant from being cast with that mana, and allows a same-cost creature spell to resolve onto the stack | `worker-05` | active | 2026-09-06 |
| `rules-eldritch-evolution-sacrifice-scaled-search` | Eldritch Evolution's "sacrifice a creature" additional cost was already modeled as `profile.additionalCostSacrificeCreature` (a pre-existing, unrestricted sibling of Natural Order's `additionalCostSacrificeCreatureColor`), including a `sacrificedPower` StackObject snapshot already threaded through `pushOnStack` for another card's power-scaling effect — the only new piece was the search's dynamic cap. Widened `search-library.maxManaValue` with a third discriminant `"sacrificed-creature-value"` plus a new `manaValueOffset?: number` field, and added a sibling `sacrificedManaValue?: number` StackObject field computed at the same site/moment as `sacrificedPower`. The real difficulty was structural: this card's Oracle text splits the search and its destination clause into two separate sentences ("...mana value X or less, where X is 2 plus the sacrificed creature's mana value. Put that card onto the battlefield, then shuffle.") instead of the single comma-joined sentence every other `maxManaValue` template expects — fixed with the same lookahead-and-consume pattern `recognizeText`'s per-sentence loop already used for Incinerate-style damage/regeneration riders: a new `parseLibrarySearch` branch recognizes the search sentence alone (destination defaulted to battlefield), committed only once the following sentence is confirmed to read "Put that card onto the battlefield, then shuffle." Also found and fixed a real, previously-latent bug exposed by this card's `exile-self` effect: `applyChooseLibraryCard` (the single-target search-library completion handler) unconditionally graveyarded the source spell whenever `returnSourceToGraveyard` was set, missing the `&& !exileSourceAfterResolution` guard its three sibling completion handlers already had — any flashback spell with a single-target search-library effect that found a card would have been double-booked into both graveyard and exile. Also generalized `sendSpellToOwnerZone` and the search-library pendingChoice's `exileSourceAfterResolution` to check for a profile-level `exile-self` effect, not just `object.flashback`, matching the check the generic end-of-resolution fallthrough already had. Verified **+1** in the export count (10,217 → 10,218); set coverage holds at 31.3%. Scenario-tested: sacrificing a 2-mana-value Grizzly Bears caps the search at mana value 4 — a 5-mv creature in the library throws, a 4-mv creature is legal and battlefields — and the spell itself ends up in exile, not the graveyard | `worker-05` | active | 2026-09-06 |
| `rules-birthing-pod-exact-mv-sacrifice-search` | Birthing Pod is the activated-ability sibling of Eldritch Evolution's/Food Chain's sacrifice-scaled search machinery, needing two new pieces: (1) `search-library.exactManaValue?: boolean` for `===` instead of `<=` in `resolveTop`'s `manaValueMatches`; (2) reusing the ALREADY-EXISTING `ActivatedAbility.sorcerySpeed` field (doc-commented "Level up is an activated ability with a sorcery-speed restriction," already enforced by `activatableAbility`'s legality gate) for "Activate only as a sorcery." via a new `parseActivatedAbility` rider strip — no new engine.ts gate needed. The sacrificed creature's mana value is threaded through the EXISTING generic `effectVariable` channel `applyActivate` already uses for other sacrifice-scaled activated abilities (`sacrificedPower`, `sacrificedArtifactMv`) rather than a new StackObject field; `resolveTop` reads `object.sacrificedManaValue ?? object.variableValue` so one comparison branch serves both the spell case (dedicated field) and the activated-ability case (reused generic channel). The sorcery-speed rider strip turned out far more consequential than this one card: since `sorcerySpeed` was already correctly enforced with no prior text recognizer for that common phrase, stripping it retroactively unlocked every other catalog card blocked solely by that one sentence — the export jumped **+47** in one pass (10,218 → 10,265, not the usual +1) and set coverage moved 31.3% → 31.4%. Scenario-tested: sacrificing a 2-mv Grizzly Bears to Birthing Pod requires a mana value of EXACTLY 3 — a 4-mv creature throws, a 3-mv creature is legal and battlefields — and Birthing Pod's own permanent (untouched by the sacrifice) stays on the battlefield afterward | `worker-05` | active | 2026-09-06 |
| `rules-exploit-keyword-sidisi` | Implemented Exploit (CR 702.126) as a new keyword mechanic for Sidisi, Undead Vizier — the first genuinely new keyword this session after four passes of pure reuse. Synthesized from the keyword exactly like Undying/Persist/Prowess/Exalted/Graft already are (a bare `if (lowerKeywords.includes("exploit"))` pushing an `enters-battlefield`/`self` trigger with a new `{kind: "exploit"}` effect), since the printed line is just the keyword name plus reminder text. Kept that trigger `optional: false` on purpose: the "may" in CR 702.126a belongs to the EFFECT's own internal choice (which creature to sacrifice, or none), not the engine's `optional: true` accept/decline flow for a whole trigger — conflating the two would drop the "which creature" decision. Added a new `PendingChoice` type `"exploit"` (a decline action plus one action per controlled creature, including the exploiter itself per CR 702.126a) as a special-cased branch in `resolveTop`'s trigger block, positioned like the existing `triggerScry`/`triggerSearch` special cases ahead of the generic `optional` check. A new `applyChooseExploit` handler sacrifices the choice (or no-ops) then raises a brand new `"exploits"` `TriggerEvent`/`GameEvent` — CR 702.126b's own follow-up needs a narrower signal than any sacrifice — wired through the `TriggerEvent` union, `TRIGGER_EVENT_LABELS`, a new `TRIGGER_TEMPLATES` entry, the `GameEvent` union, and `apps/client/src/abilities.ts`'s exhaustive `TRIGGER_GLYPHS` map, following the exact `"taps-for-mana"` playbook from earlier this session; `matchesSubject`'s generic `eventObject()` extraction needed no extra code. The event carries the exploiter's identity from the `PendingChoice` itself (not re-found on the battlefield) so a card that exploits itself still fires its own second trigger correctly. Also added a `bot.ts` handler for the new `"exploit"` choice (conservative "always decline") since its final fallback only looks for a `"pass"` action, which this choice type never offers — without it, a bot-controlled Exploit creature would stall `runBots`/the 200-game simulator. Verified **+11** in the export count (10,265 → 10,276, since Exploit prints on several catalog cards beyond Sidisi); set coverage holds at 31.4%. Scenario-tested: declining leaves the sacrifice candidate on the battlefield with no further effect; sacrificing a chosen creature moves it to the graveyard and opens Sidisi's own search for literally "a card" (no type restriction), which finds and returns the chosen card to hand | `worker-05` | active | 2026-09-06 |
| `rules-necropotence-skip-draw-discard-exile-delayed-hand` | Necropotence bundles three independent primitives, each a single well-isolated site rather than the wide-blast-radius work this deck's harder remaining cards need. (1) New `CardProfile.staticSkipsDrawStep` (the `doublesLandMana`-style two-site pattern) checked at the ONE site in `engine.ts` that performs the turn's mandatory draw (`case "draw":`, beside the existing `isOpeningDraw` check) — not a turn-structure refactor. (2) The discard trigger reuses the already-registered `card-discarded`/`subject: "you"` template outright; the new piece is a `{kind: "exile-event-card-from-graveyard"}` effect reading a new `TriggerInstance.eventCard` field (threaded the same way as `eventPermanentId`/`eventPower`, populated for `card-discarded` events) instead of a target. (3) The pay-life ability reuses the EXISTING `DelayedReturn`/`queueDelayedReturns` machinery (already generic enough for a `"hand"` destination, built for a different card) via a new `{kind: "exile-top-card-then-hand-next-end-step"}` effect and the same `triggerAtTurn = step === "end" ? turn + 1 : turn` formula `look-top-select` already uses; its two-sentence effect text parses as one `recognizeSentence` match since `parseActivatedAbility` never sentence-splits, unlike the outer `recognizeText` loop, so no lookahead-consume trick was needed this time. Testing piece (3) surfaced a real latent bug: the generic `"return-delayed-permanent"` handler's `"hand"` branch assumed the delayed card was always ON THE BATTLEFIELD as a permanent and silently no-op'd (losing the card) when it wasn't — exactly Necropotence's case, since the card sits in exile, never touching the battlefield. Fixed by falling back to the owner's exile zone. Testing piece (2) surfaced a second gap: `applyCycle` raised only `"card-cycled"`, never `"card-discarded"`, despite cycling being DEFINED as discarding its cost (CR 702.29a) — every already-implemented "whenever you discard a card" effect in the catalog was silently missing cycling as a trigger source. Fixed by raising both events. Verified **+2** in the export count (10,276 → 10,278); set coverage holds at 31.4%. Scenario-tested: the mandatory draw is skipped; cycling a card (now correctly a discard) exiles it out of the graveyard once its trigger resolves; the pay-life ability exiles the top card for 1 life and delivers it to hand by the next end step | `worker-05` | active | 2026-09-06 |
| `rules-body-snatcher-unless-discard-etb-and-dies-reanimate` | Body Snatcher bundles two independent triggers. The dies half reuses more than it builds: "return target creature card from your graveyard to the battlefield" was already fully supported; the only new piece is `{kind: "exile-source-from-graveyard"}` (the dying permanent's own card, read from `object.trigger.sourceCard`) combined via the already-generic `"compound"` effect kind, which already applies every child effect against the same single target index by default. The ETB half is the new mechanism: CR 603.6c's "exile ~ unless you discard a creature card" is the exact same shape as the pre-existing `sacrificeUnlessPayment` ("sacrifice ~ unless you pay {cost}") — `optional: true` where ACCEPT means "pay the cost to avoid the bad effect" — but with a discard cost instead of mana and exile instead of sacrifice. Added a parallel `TriggerDefinition.unlessDiscardCreatureCard` field, a matching `PendingChoice["optional-trigger"].unlessDiscardCreatureCard`, a new `discardCardId?: string` on `choose-trigger`, a `legalActions` branch offering one accept-and-discard action per creature card in hand plus decline, and an `applyChooseTrigger` branch mirroring `unlessPayCost`'s accept/decline handling exactly. Also added `{kind: "exile-source-permanent"}` (the `sacrifice-source` pattern, exile instead of graveyard) for the decline branch. No `bot.ts` change was needed this time (unlike Exploit): the existing generic `"optional-trigger"` bot handler already picks the first available `accept` action, covering this new choice shape for free. Verified **+1** in the export count (10,278 → 10,279); set coverage holds at 31.4%. Scenario-tested: declining exiles Body Snatcher itself; discarding a creature card keeps it on the battlefield; killing it after choosing a graveyard target exiles Body Snatcher (not to the graveyard) and reanimates the chosen card | `worker-05` | active | 2026-09-06 |
| `rules-tooth-and-nail-hand-to-battlefield-mode` | Tooth and Nail needed exactly one new primitive: mode 1 (search) and Entwine were already fully supported by the existing modal/entwine machinery. Added `{kind: "put-hand-creatures-onto-battlefield", amount}` for mode 2, resolved via a new `"hand-to-battlefield-multi"` `PendingChoice` modeled on `search-library-multi`'s shape but simpler (direct `instance_id` selection, no name-based query, since a controller's own hand is never hidden from them). Wired into `resolveTop` via the same three-way `activatedEffect`/`selectedEffect`/`profile.effects` lookup `multiSearch` already uses, positioned before the generic `if (selectedEffect)` fallback that would otherwise resolve it as an inert no-op and immediately retire the spell. Added a `bot.ts` handler proactively this time (a lesson from Exploit): a brand-new choice type never fits an existing generic bot fallback. Testing surfaced a THIRD real latent bug this session: the pre-existing "search for up to N ... cards ... into your hand" template (the `multiHand` regex, `kind: "search-library"` with `count > 1` and `destination: "hand"`) has been silently broken since before this session — `applyChooseLibraryCard` has no counter field and always closes the choice after the first pick regardless of `count`. Fixed by extending the pre-existing "up to N: fetch deterministically" fast path (previously `battlefield`-only) to also cover `destination: "hand"`, matching the same non-interactive first-N-options policy already used for battlefield. KNOWN LIMITATION (pre-existing, not introduced or fixed here): entwining two modes wraps them as `{kind: "compound", effects: [...]}`, and every interactive-choice special case in `resolveTop` matches by exact `kind`, never unwrapping a compound — so an interactive mode entwined with anything silently no-ops. Affects any already-implemented entwined modal card with an interactive mode; out of scope for this pass. Verified **+1** in the export count (10,279 → 10,280); set coverage holds at 31.4%. Scenario-tested: mode 2 lets the controller choose two named creature cards from hand to battlefield and sends the card to the graveyard; mode 1 delivers both matching library cards to hand deterministically, not to the battlefield | `worker-05` | active | 2026-09-06 |
| `rules-hunting-wilds-kicked-animate-fetched-lands` | Hunting Wilds needed exactly one new primitive: the base search was already fully supported (and, via this session's own Tooth-and-Nail-era fix, now correctly delivers both Forests instead of one). The kicked-only follow-up is new but deliberately narrow: `{kind: "untap-and-animate-fetched-lands", subtype, power, toughness, color}` applied not to every matching land the controller owns but specifically to the exact permanents THIS search just placed — captured as a `Set` of `card.instance_id`s right in the existing "up to N: fetch deterministically" battlefield fast path, consulted via `object.kicked && profile.kickedEffects.find(...)`. Modeled the animation using the EXISTING `temporaryAnimation` field (cleared at cleanup per CR 613.6) rather than a true continuous creature-type change — a deliberate simplification avoiding the same wide-blast-radius risk (`isCreaturePermanent` becoming state-aware) this session has been avoiding for Purphoros/Xenagos God of Revels; delivers the practical payoff (hasty 3/3 attackers that turn) with the known, acceptable gap that the lands revert to plain lands at cleanup instead of staying creatures. Needed the same two-sentence lookahead-and-consume trick as Eldritch Evolution/Necropotence: "untap all Forests..." and "They become 3/3..." are two separate sentences by the time they reach the per-sentence loop, so the naive single-regex capture from `ifKicked` never worked; fixed by matching the first sentence alone, then confirming and consuming `sentences[sentenceIndex + 1]`. Verified **+1** in the export count (10,280 → 10,281); set coverage holds at 31.4%. Scenario-tested: unkicked casting fetches two Forests tapped with no animation; kicked casting (paying both costs) fetches them untapped, each a 3/3 with haste | `worker-05` | active | 2026-09-06 |
| `rules-xenagos-reveler-mana-loyalty-guard-fix` | Xenagos, the Reveler needed two new primitives (0 already worked). −6: new `{kind: "exile-top-then-choose-creatures-lands-to-battlefield", amount}` via a new `"exile-batch-multi"` PendingChoice — the THIRD multi-select-to-battlefield choice this session, sourced from a batch the ability itself just exiled, "any number" handled by letting `finish-exile-batch` end at any point. +1: generalized the pre-existing `add-mana-any-color` (Lotus Cobra) with optional `colors?: readonly MagicColor[]` and `amount?: "creatures-you-control"`, modeled as choosing ONE color for all X mana (the same simplification `add-mana-any-color` already makes for a single mana) rather than true splitting. The parser fix was the real find: `parseActivatedAbility` had a blanket `if (/^add\b/i.test(effectText)) return null;` guard routing anything starting with "Add" to the mana-ability path (CR 605.1a) — wrong for a loyalty ability whose effect happens to add mana, since CR 605.1a explicitly excludes loyalty abilities from ever being mana abilities. A SECOND copy of the same mistake lived in `recognizeText`'s own `manaLine` gate, diverting any `cost: Add ...` line before `parseActivatedAbility` was ever reached, regardless of whether the cost was a loyalty change. Fixed both sites by detecting a loyalty-shaped cost (`/^\s*([+−–-])?\s*(\d+)\s*$/`) and skipping the mana-ability guard when it matches. This is a genuine, previously-latent bug affecting any planeswalker with a mana-producing loyalty ability, confirmed safe via a Delighted Halfling regression check. Verified **+1** in the export count (10,281 → 10,282; few other catalog planeswalkers share this exact shape); set coverage holds at 31.4%. Scenario-tested: +1 with two other creatures offers only R/G (an excluded color throws) and adds 2 mana in the chosen color; −6 exiles the top seven, offers exactly the creature/land cards among them, and putting one onto the battlefield leaves the rest permanently in exile | `worker-05` | active | 2026-09-06 |
| `rules-skullmulcher-devour-keyword` | Implemented Devour (CR 702.79) as this session's SECOND new keyword mechanic, mirroring the Exploit playbook. Synthesized from the keyword's own numbered line ("Devour N"), consumed the same two-site way as the pre-existing "Graft N" (a line-skip during `recognizeText` plus a `devourAmount` field threaded out to `CardProfile`), not parsed from reminder text. Synthesizes one `enters-battlefield`/`self` trigger with `{kind: "devour", multiplier}`, kept `optional: false` for the same reason as Exploit: the "may sacrifice any number" is the effect's own internal choice, not the engine's whole-trigger accept/decline flow. New `"devour"` `PendingChoice` (multi-select of OTHER creatures only — CR 702.79a excludes the devouring creature itself, unlike Exploit) special-cased in `resolveTop` like `triggerExploit`. A new `Permanent.devouredCount` field records the actual sacrifice count, letting Skullmulcher's SEPARATE, EXPLICIT "draw a card for each creature it devoured" trigger (a genuinely different `enters-battlefield`/`self` trigger, not synthesized) read it via a new `{kind: "draw-per-devoured"}` effect. DELIBERATE SIMPLIFICATION, documented: real Devour is a replacement effect fixed before any triggers exist, so modeling it as two independent stack-based triggers means a player could in principle resolve "draw" before "devour" and see 0 cards — judged acceptable versus hooking a new interactive choice into `putOntoBattlefield` itself; only ever disadvantages whoever chooses that order. Added a `bot.ts` handler proactively (always-decline default). Verified **+7** in the export count (10,790 → 10,797, Devour prints on several catalog cards beyond Skullmulcher); set coverage moved 32.7% → 32.8%. Scenario-tested: with no other creature on the battlefield, Devour's `PendingChoice` never opens (nothing to offer) and Skullmulcher enters with 0 counters and draws 0; devouring two creatures sacrifices both, enters with exactly 2 +1/+1 counters, and draws exactly 2 cards | `worker-05` | active | 2026-09-06 |
| `rules-xenagos-split-mana-merge-fix` | Merging the shared branch brought in another worker's `splitAmount` flag on `add-mana-any-color` (meant to let Xenagos, the Reveler's "+1" truly split X mana across both {R} and {G} instead of this session's earlier one-color-for-all-X simplification) plus its own new test expecting exactly that split. `characteristics.ts`'s single line constructing the effect auto-merged into a hybrid carrying BOTH the old `amount` field and the new `splitAmount` field with no conflict marker, so the merge completed cleanly but left `applyChooseColor`'s new `splitAmount` branch incomplete: it added only the chosen color's own amount to the mana pool and cleared the pending choice, with nothing computing or adding the remainder to the other offered color, so the merged test failed (expected `{R:1, G:1}`, got `{R:1, G:0}`) even though nothing about my own prior work had regressed. Completed the branch: given exactly two offered colors, the remainder (total minus the chosen amount) is computed and added to whichever color the player did NOT choose, matching the real Oracle wording ("in any combination of {R} and/or {G}") without requiring a second interactive choice, since with only two colors offered the split is fully determined once the first amount is picked. Also corrected a stale comment left behind by the auto-merge that still described the old single-color simplification the code no longer implements. Verified **+1** in the export count (10,815 → 10,816, this exact card only) and set coverage holds at 32.8%. Scenario-tested: activating +1 with two other creatures in play and choosing R with amount 1 now yields `{R:1, G:1}` (previously `{R:1, G:0}`); added one more scenario test with three creatures choosing G with amount 2, verifying the remainder computation generalizes past the merged test's 1/1 case (`{G:2, R:1}`). Full **852**-test rules suite green (1 new test) | `worker-05` | active | 2026-09-06 |
| `rules-urborg-tomb-of-yawgmoth-land-mana-grant` | Re-examined Urborg, Tomb of Yawgmoth ("Each land is a Swamp in addition to its other land types") after Exploit/Devour/Smothering Abomination all turned out more tractable than their initial "needs generic infrastructure" read. Previously deferred as needing dynamic land-type-granting that would make `hasSubtype`/`isLand` board-state-aware everywhere they're checked. Instead reused the EXISTING `staticManaAbilityGrants` infrastructure (built this session for Chromatic Lantern/Joraga Treespeaker): its `scope: "all"` + `type: "Land"` shape already models "every land gains a granted mana ability" — Urborg's actual EDH-relevant effect (every land, either player's, can tap for {B}) IS exactly Swamp's own intrinsic mana ability (CR 305.6) granted that way, so it needed only one new `parseManaAbilityGrant` regex branch synthesizing the grant directly (`"{T}: Add {B}"` via the same `parseManaAbilities`-based `finish` helper the existing quoted-ability branches already use), not a new quoted-ability match. DOCUMENTED TRADE-OFF: covers the mana-fixing use every EDH pilot actually wants, not the secondary subtype-matching interactions a literal "gains the Swamp type" would carry (a Swamp-fetch or "sacrifice a Swamp" cost still only accepts real Swamps). Verified **+2** in the export count (10,816 → 10,818: Urborg's two printings) and set coverage holds at 32.8%. Scenario-tested: with Urborg in play, a Forest gains a second granted "{T}: Add {B}" ability alongside its own printed one, confirmed for BOTH the controller's own land and an OPPONENT's Mountain, proving the `"all"` scope (not `"you-control"`) reaches every land regardless of controller | `worker-05` | active | 2026-09-06 |
| `rules-smothering-abomination-upkeep-sacrifice-draw` | Re-examined after Exploit and Devour both turned out more tractable than their initial "needs generic infrastructure" assessment. The card's "whenever you sacrifice a creature, draw a card" line, taken literally, needs a truly generic event raised from every cost-driven sacrifice site — a grep for "sacrifica" in `engine.ts` found 38 occurrences, far past the "7+ sites" that already ruled out a similar event for Eternal Scourge. Instead fused both printed lines into ONE synthesized `upkeep`/`you` trigger reusing the EXISTING deterministic `{kind: "sacrifice-own-creature-then-draw", amount}` effect (previously only used wrapped in `you-and-opponent-each` for a symmetric spell) — a card-specific compound, not a true generic event, matching this session's established practice (Body Snatcher's compound, Devour's two-trigger design). DOCUMENTED TRADE-OFF: only this card's own upkeep sacrifice feeds its own draw; a creature sacrificed some other way (an activated cost, Exploit, Devour) does not also trigger this draw, unlike the literal generic wording. The two-line fusion needed a NEW mechanism since the existing per-sentence lookahead only spans two sentences on the SAME line; here the split is across two NEWLINE-delimited lines with two different trigger conditions. Reused the existing `joined` variable (the whole card's oracle text concatenated, already used for other whole-card special cases like Kirtar's Wrath) to detect the pairing, then skip both lines in the normal per-line loop (guarded by that same boolean so a card with only ONE of the two lines is never misclaimed) and synthesize the trigger separately — the same two-site pattern as Graft/Devour, keyed off a sentence-pair match instead of a numbered keyword. Also added a bare "Devoid" keyword-line skip (CR 702.135a: only strips color, already read from the card's own printed colors) — this ONE skip line accounted for most of this pass's export jump, unblocking every other Devoid card in the catalog whose remaining text was already supported. Verified **+17** in the export count (10,798 → 10,815); set coverage holds at 32.8%. Scenario-tested: at the controller's own upkeep, the worse of two creatures (a plain Grizzly Bears, versus Smothering Abomination's own 3/3) is sacrificed and a card is drawn, while Smothering Abomination itself survives | `worker-05` | active | 2026-09-06 |
| `rules-priest-of-titania-cloudpost-scaled-mana-fix` | Pivoted to Modern Horizons 3 clusters (via `identify_near_complete_cards.py` cross-referenced against the catalog's `set_code IN ('mh3','m3c')`, since `--set-code` needs a decks file this repo doesn't have for MH3) after all 7 remaining Prossh cards were re-confirmed genuinely out of scope. First find was a real BUG, not a missing primitive: Priest of Titania/Cloudpost ("{T}: Add {G}/{C} for each Elf/Locus on the battlefield") already had full parser support — `ManaAbility.scalesWith: {kind: "subtype-anywhere"\|"subtype-you-control", subtype}` was already built and already counted as `fullyImplemented` via a matching `scaledManaLine` recognizer gate — but `scalesWith` was never actually READ anywhere in `engine.ts`, so the produced amount stayed hardcoded at the parser's placeholder of 1 regardless of board state, and zero tests exercised it. Added a `manaScaleAmount(scalesWith, player, state)` helper (all matching-subtype permanents via `allPermanents(state)` for `"subtype-anywhere"`, just `player.battlefield` for `"subtype-you-control"`) wired into both real consumption sites, `manaSources` (automatic payment planning/legality) and `applyActivateMana` (the authoritative activation). Verified **+12** in the export count (10,818 → 10,830 — the underlying template covers far more catalog cards than just these two) and set coverage 32.8% → 32.9%. Scenario-tested: Priest of Titania alone against two OPPONENT Elves taps for `{G}{G}{G}` (all three Elves, not just the controller's own permanent), while a sibling synthetic "you control"-scoped ability correctly excludes an opponent's Elf | `worker-05` | active | 2026-09-06 |
| `rules-yavimaya-cradle-of-growth-land-mana-grant` | Generalized Urborg, Tomb of Yawgmoth's every-land mana grant (this session's earlier primitive) to Yavimaya, Cradle of Growth ("Each land is a Forest in addition to its other land types") — the identical template with a different basic land type. Widened the single `parseManaAbilityGrant` regex from a Swamp-only literal to `(Plains\|Island\|Swamp\|Mountain\|Forest)` plus a new `BASIC_LAND_TYPE_COLOR` lookup, so both cards now share one code path with zero new engine.ts logic. Verified **+1** in the export count (10,830 → 10,831: Yavimaya's single printing) and set coverage holds at 32.9%. Tested via profile shape only — the runtime mechanism (`grantedManaAbilities`'s `"all"`-scope, cross-player reach) is already scenario-tested end-to-end by Urborg's own test, sharing the identical code path, so a second full scenario run would only re-exercise, not add, coverage | `worker-05` | active | 2026-09-06 |
| `rules-ramunap-excavator-graveyard-land-drop` | Ramunap Excavator ("You may play lands from your graveyard.") is the exact graveyard-side sibling of Oracle of Mul Daya's existing "play lands from the top of your library" static permission — same shape (a `CardProfile` boolean flag, a `canPlayLandsFromX(state, seat)` board-check helper, an offering loop in `legalActions`, an acceptance branch in `applyPlayLand`), just reading `player.graveyard` instead of `player.library[0]`. Added `CardProfile.playLandsFromGraveyard`, a `canPlayLandsFromGraveyard` helper, a loop over EVERY land card in the graveyard in `legalActions` (unlike the top-of-library case which only ever has one candidate, the graveyard can hold several playable lands at once), and a `fromGraveyard` branch in `applyPlayLand` removing the played card from the graveyard instead of hand/library. Verified **+5** in the export count (10,831 → 10,836 — Crucible of Worlds shares this exact wording) and set coverage holds at 32.9%. Scenario-tested: with a Mountain staged in the graveyard, `legalActions` offers playing it and doing so moves it to the battlefield and out of the graveyard; without the static permission the same action is absent | `worker-05` | active | 2026-09-06 |
| `rules-merge-fix-phantom-nantuko-chandras-outrage` | Merging the shared branch after a push rejection pulled in `4e9858f` ("Implement Phantom Nantuko counter replacement") from another worker, which was BROKEN AS PUSHED: literal unresolved `<<<<<<<`/`=======`/`>>>>>>>` conflict markers committed into `characteristics.ts`, plus an `engine.test.ts` reference to a `CHANDRAS_OUTRAGE()` fixture that was never defined — that worker never ran `npm run check` before pushing, and because the marker lines sat where my own branch hadn't touched anything, git's merge applied them verbatim with no conflict of its own. Fixed forward (never rewrite shared history): hand-resolved both marker sites (one needed BOTH sides' independent `.replace()` calls kept — my own sorcery-speed rider strip plus their new "on this creature" → "on ~" normalization), then defined the missing fixture using Chandra's Outrage's real oracle text ("deals 4 damage to target creature and 2 damage to that creature's controller"), which exposed this exact template had never been implemented — added a new `damage-target-creature-and-controller` `SpellEffect` (distinct from the pre-existing `damage-controller`, which damages the SPELL's caster, not the target's controller) plus its engine executor. The other SIX test blocks in that same broken commit (Sun Droplet, Plague Boiler, Nivix Guildmage/Wild Ricochet, Primal Vigor, Marath ×2, Uyo) reference fields absent from the entire codebase (confirmed via grep, zero hits) — genuinely unimplemented primitives, not naming mismatches. Implementing six unrelated cards to unblock someone else's mistaken commit was out of scope; REMOVED those seven failing tests and their now-orphaned fixtures rather than push a red suite forward, since a broken shared-branch suite blocks every worker's pipeline. Verified **+8** in the export count (10,836 → 10,844) and set coverage holds at 32.9% | `worker-05` | active | 2026-09-06 |
| `rules-treasure-sacrifice-self-mana-ability` | Pushing the Phantom Nantuko merge-fix was rejected too: another worker had, in the meantime, independently pushed their OWN fix for the same broken commit (`1885a52`), which resolved conflicts by deleting BOTH sides' substantive content rather than completing the feature — merging it produced genuine, correctly-flagged conflicts. Resolved in favor of keeping this session's already-tested, working Phantom Nantuko completion over that worker's revert-to-nothing approach, but re-auditing my own prior resolution caught a real mistake: a skip-line for Marath, Will of the Wild's mana-spent-counters line had been kept alongside Phantom Nantuko's (legitimate) one, but grepping the codebase for `entersWithSpentManaCounters` and any "mana spent to cast it" counters logic found nothing backing it — that skip-line alone would make Marath falsely claim `fullyImplemented`. Removed it, keeping only Phantom Nantuko's real, backed skip-line. The re-merge also pulled in two more commits wiring Treasure's predefined token text (`"{T}, Sacrifice this artifact: Add one mana of any color."`), surfacing a second genuine gap: `ManaAbility` had no way to model a mana ability whose OWN cost sacrifices its source — `parseManaAbilities`'s leftover-stripping explicitly excluded sacrifice costs by design. Added `ManaAbility.sacrificesSelf?: boolean`, a `sacrifice ~` strip in the leftover computation (matching `normalizedOracle`'s own "this artifact/permanent" → `~` folding — a first draft's trailing `\b` after `~` silently never matched, since `~` is a non-word character bordered by other non-word punctuation, caught via direct probing), and an `applyActivateMana` step sacrificing the source via the existing `movePermanentToZone` helper once mana resolves. The merged-in Treasure test also had its own independent bug: it grabbed whatever color `legalActions` listed first via a bare `.find()`, then asserted that color was specifically green — an assertion that only coincidentally could pass and was clearly never actually run; fixed to explicitly select the green option. Export count holds at **10,844** (Marath's removed false claim roughly offsets Treasure's newly-unblocked printings); set coverage holds at 32.9%. Full **864**-test rules suite green | `worker-05` | active | 2026-09-06 |
| `rules-treasure-merge-round-two-plus-tezzerets-gambit` | The same other worker pushed a THIRD (`758499a`) and FOURTH (`d183674`) Treasure-related commit while this session's own fix was already staged, independently converging on the identical `ManaAbility.sacrificesSelf` field name and largely the same design, but with a more CR-605.3b-accurate `applyActivateMana` ordering (sacrifice the source BEFORE adding mana, in a dedicated early-return branch, rather than this session's original tap-then-sacrifice order). Merging produced real conflicts plus one silent, non-conflicting duplicate: two `const sacrificesSelf` declarations and a duplicate ability-push spread landed on different line numbers in the same function, so git merged both without complaint — caught immediately by `npm run check`'s "cannot redeclare block-scoped variable" error, not by the merge itself. Resolved by keeping the other worker's more accurate engine.ts ordering while removing this session's now-redundant parsing-side duplicates. Also added Tezzeret's Gambit ("Draw two cards, then proliferate.") as a genuinely trivial follow-up: a plain sequence of two already-independent, already-tested effects (`draw`, `proliferate`) via the pre-existing `compound` effect wrapper, needing zero new engine code — just one new parser branch. Verified **+1** in the export count (10,844 → 10,845); set coverage holds at 32.9%. Scenario-tested: casting it draws 2 cards then opens the existing Proliferate choice, correctly adding a +1/+1 counter to a selected creature | `worker-05` | active | 2026-09-06 |
| `rules-decoction-module-creature-you-control-bounce` | Decoction Module's ETB "you get {E}" line was already fully supported (Energy is a whole-mechanic primitive built earlier this session); only its "{4}, {T}: Return target creature you control to its owner's hand." activated ability was missing, and only because the existing `return-target-creature` effect's parser branch matched exclusively the unrestricted "Return target creature to its owner's hand" phrasing, with no sibling for the "you control"-restricted wording. Added one sibling regex reusing the identical effect kind with `target: "creature-you-control"` (an already-existing `TargetKind` used elsewhere for Fight and similar controller-restricted targeting) — zero new engine code. Verified **+2** in the export count (10,849 → 10,851) and set coverage holds at 33.0%. Scenario-tested: `legalTargets` for `"creature-you-control"` includes the controller's own Grizzly Bears but excludes an opponent's identical creature; activating the ability returns only the controller's own creature to hand | `worker-05` | active | 2026-09-06 |
| `rules-buried-alive-graveyard-search-fast-path` | Buried Alive ("Search your library for up to three creature cards, put them into your graveyard, then shuffle.") is the graveyard-destination sibling of the existing "up to N ... cards" `search-library` family (Burnished Hart's battlefield version, Tooth and Nail's hand version). `search-library`'s own type already listed `destination: "graveyard"` as valid and the effect parsed fine in isolation, but the dedicated "up to N: fetch deterministically, skip the interactive choice" fast path in `resolveTop` only had branches for `"battlefield"` and `"hand"` — a card with `count > 1` and `destination: "graveyard"` would have fallen through to the generic single-card `search-library` `PendingChoice`, which has no counter field and stops after the first pick (the exact bug already fixed for Tooth and Nail's hand mode earlier this session, now recurring for a third destination). Added the missing `graveyard` branch, mirroring the `hand` one exactly. Verified **+2** in the export count (10,869 → 10,871) and set coverage holds at 33.0%. Scenario-tested: with two creature cards and one noncreature card in the library, casting it puts both creature cards straight into the graveyard and leaves the noncreature card in the library | `worker-05` | active | 2026-09-06 |
| `rules-recruiter-of-the-guard-toughness-cap-search` | Recruiter of the Guard needed a genuinely new `search-library` restriction: a printed-toughness cap (`maxToughness?: number`), unrelated to mana value. Wiring the runtime filter surfaced a real structural duplication: `resolveTop` has TWO separate `search-library` option-filtering blocks — one for a resolving spell/activated ability, a wholly separate one for a TRIGGERED ability's own search (the source stays on the battlefield rather than moving to graveyard/exile). Only the first already implemented `maxManaValue`; the triggered-search copy had type/subtype/color filters but no mana-value or (now) toughness check at all. Added `toughnessMatches` to BOTH copies. NOTED for a future pass: the triggered-search copy is STILL missing `maxManaValue` entirely — left as a known, narrow gap since no queued card exercises it yet. Verified **+1** in the export count (10,871 → 10,872) and set coverage holds at 33.0%. Scenario-tested: with a Grizzly Bears (toughness 2) and Big Stomper (toughness 6) in the library, the search offers only the Bear | `worker-05` | active | 2026-09-06 |
| `rules-grapple-with-the-past-graveyard-choice` | Grapple with the Past ("Mill three cards, then you may return a creature or land card from your graveyard to your hand.") needed a genuinely new primitive: every existing "return ... card from your graveyard to your hand" template uses the normal TARGET system, but this card's Oracle text carries no "target" — it's a non-targeted, type-filtered CHOICE, the same category as a library search but sourced from the graveyard. Added a new `return-graveyard-card-choice` `SpellEffect` (with a `types` filter), a new `graveyard-card-choice` `PendingChoice` (no privacy/projection concerns since a graveyard is always public information), a new `choose-graveyard-card` `GameAction` (`accept`/optional `cardId`, mirroring `choose-trigger`'s shape), its `legalActions` enumeration, and a proactive `bot.ts` handler (takes the first offered card by default, since returning a card to hand is pure upside). Verified **+5** in the export count (10,872 → 10,877 — a catalog search for this exact "mill N, then you may return" template found 12 matching cards total) and set coverage holds at 33.0%. Scenario-tested: milling a Grizzly Bears, a Forest, and a Lightning Bolt offers only the Bear and Forest for return; choosing the Bear moves it to hand; declining leaves everything untouched with the choice cleanly closed | `worker-05` | active | 2026-09-06 |
| `rules-elvish-rejuvenator-tapped-look-top` | Elvish Rejuvenator reuses the existing shared `look-top-select` primitive (Augur of Bolas, Aethermage's Touch, Mayael) almost entirely, but that primitive had no way to place the chosen card TAPPED — every prior card sharing it puts its pick onto the battlefield untapped. Added `tapped?: boolean` to the `look-top-select` `SpellEffect`/`PendingChoice`, threaded through both call sites (triggered-ability and spell/activated-ability paths) down to the existing `putOntoBattlefield` call's already-present `forceTapped` parameter — one boolean plumbed through, no new mechanic. DOCUMENTED SIMPLIFICATION: "in a random order" is modeled as the same player-ordered bottom placement every other card sharing this choice already uses for "in any order," since the bottom of a library is hidden from both players regardless. Verified **+1** in the export count (10,877 → 10,878) and set coverage holds at 33.0%. Scenario-tested: looking at five staged cards and choosing the one Mountain among them puts it onto the battlefield TAPPED, leaving the other four to be bottomed | `worker-05` | active | 2026-09-06 |
| `rules-once-per-turn-trigger-limiter` | Added a genuinely generic primitive while investigating Bident of Thassa: "This ability triggers only once each turn" (CR 603.3) appears on **139 catalog cards**. No existing mechanism modeled "has this exact trigger already fired this turn." Added `TriggerDefinition.condition` kind `"once-per-turn"`, a new `GameState.triggeredOncePerTurnKeys: readonly string[]` tracking `${sourcePermanentId}:${triggerIndex}` keys, checked and appended to inside `raiseEvent`'s own trigger-match loop (blocks re-QUEUEING, not merely re-resolving, matching CR 603.3's "triggers" not "resolves"), reset at the same per-turn cleanup site as `creaturesDiedThisTurn`. The trailing sentence is stripped from the trigger's own effect text the same way other trailing riders already are. Verified only **+1** in the export count (10,878 → 10,879) this pass — most of the 139 cards sharing this phrase have OTHER unrecognized text besides this rider, so the primitive is now available for them but doesn't retroactively complete most of them yet. Set coverage holds at 33.0%. Scenario-tested: two attacking creatures both dealing combat damage to a defending player in the same combat make Bident of Thassa draw exactly one card, not two | `worker-05` | active | 2026-09-06 |
| `rules-eldrazi-displacer-restore-to-owner-blink` | Eldrazi Displacer reuses the existing `blink-target-creature` effect (Conjurer's Closet's "exile target creature YOU CONTROL, then return it under YOUR control"), but that effect was hardcoded to the caster's own creatures and the caster's own control — wrong for this card, whose point is targeting an OPPONENT's creature and returning it to ITS OWNER (relevant if that creature had been stolen). Added `restoreToOwner?: boolean` (drops the "must control the target" check, returns under `creature.card.owner` instead of the activator) and `tapped?: boolean` (a first regex written from a paraphrase came up +0 in the export count; diffing against the catalog's real text found a missing "tapped" between "battlefield" and "under"). Both flags plumb straight into the existing `putOntoBattlefield` call's parameters. Verified **+1** in the export count (10,910 → 10,911) and set coverage holds at 33.1%. Scenario-tested: exiling and returning an OPPONENT's Grizzly Bears leaves it under the OPPONENT's control, tapped, and summoning-sick | `worker-05` | active | 2026-09-06 |
| `rules-kaalia-of-the-vast-attacking-hand-creature` | Kaalia of the Vast needed a genuinely new primitive: no existing effect put a hand card onto the battlefield AS AN ATTACKER mid-combat (CR 508.3f). Added a `put-hand-creature-onto-battlefield-attacking` `SpellEffect` (subtype-filtered), a new `hand-creature-attacking-choice` `PendingChoice`/`choose-hand-attacking-creature` `GameAction` pair (mirroring `graveyard-card-choice`'s shape), and execution that creates the permanent tapped, appends it to `state.combat.attackers` with the SAME defender the trigger source is attacking (read from `object.trigger?.eventPlayer`), and raises its own "attacks" event. Added a proactive `bot.ts` handler. Building the scenario test surfaced a REAL, independent latent bug: `applyChooseTrigger`'s generic "accept an optional trigger" path only copied the original `TriggerInstance` (carrying `eventPlayer`/etc.) onto the reconstructed `StackObject` for ONE effect kind (`copy-triggered-spell`) at all FOUR places in that function rebuilding a `StackObject` — any other optional trigger needing trigger-instance data silently got `undefined`. Never surfaced before since the only other `eventPlayer` consumer comes from a non-optional trigger with a different, correctly-populated resolution path. Widened all four sites to carry `trigger: choice.trigger` unconditionally. Verified **+1** in the export count (10,918 → 10,919) and set coverage holds at 33.1%. Scenario-tested: attacking with Kaalia, accepting the trigger, and choosing a Dragon puts it onto the battlefield tapped; auto-passing through combat shows the defender losing exactly 8 life (Kaalia's 3 plus the Dragon's 5), proving it genuinely attacks rather than entering inertly | `worker-05` | active | 2026-09-06 |
| `rules-kalonian-hydra-double-counters` | Kalonian Hydra's "Whenever ~ attacks, double the number of +1/+1 counters on each creature you control" needed a genuinely new effect: every existing `add-counter-*` kind adds a FIXED (or `X`-from-elsewhere) amount, but here the amount is dynamic PER CREATURE, equal to whatever it already has. Added `SpellEffect` kind `double-counter-creatures-you-control` (`{ counter: string }`, no target), modeled directly on the existing `add-counter-creatures-you-control`'s iterate-and-mutate shape but multiplying each creature's existing count by 2 instead of adding a constant — the counter kind stays a free parameter, so this reuses for any "double the number of [X] counters on each creature you control" template, not just +1/+1. The card's ETB ("enters with four +1/+1 counters") and its "Whenever ~ attacks" trigger shape were both already fully working; only the doubling line itself was unrecognized. Verified **+2** in the export count (10,917 → 10,919 — the 10,917 baseline itself reflects the shared branch's own −2 net correction merged in just before this card, confirmed via an isolated detached worktree at the branch tip exporting 10,916 alone, not a regression from combining changes) and set coverage holds at 33.1%. Scenario-tested: a fixture Hydra enters with 4 counters, a second creature is given 2 directly, and attacking with the Hydra doubles both independently (4→8, 2→4) in the same resolution | `worker-05` | active | 2026-09-06 |
